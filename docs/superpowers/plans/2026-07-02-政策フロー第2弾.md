# 政策→フロー 第2弾 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox (`- [ ]`).

**Goal:** 財務省の対外・対内証券投資（週次）・為替介入（月次集計）と海外勢の米国債保有（FRED）を追加し、「国際資金フロー」を直接観測できるようにする。

**Architecture:** 新ソース `mof`（cp932 CSV、モジュール内キャッシュ）を fetch_macro に追加し `mof_dispatch` で kind 分岐。build に `no_stale` フラグ。マネーの流れに週次フロー比較チャート。

**Tech Stack:** Python 3.12（標準ライブラリ）、Vanilla JS、pytest。新規依存なし。

## Global Constraints
- 値は財務省公式CSV・FREDのみ。cp932固定。億円→兆円=÷10000・4桁丸め。
- week.csv: 列11=対外合計net、列22=対内合計net、列14=対内株式net。期間終了日を日付（全角`．`/`～`正規化、終了月<開始月で年+1）。
- 介入CSV: 列3=Year/列4=英語月名（空は繰越し）/列5=Day/列6=金額(億円)。列5・6が数値の行のみ日次。月次合計・実施月のみ。
- `no_stale: true` の系列は stale=False。
- 既存テストを壊さない。Gitはmain、各タスク末尾でコミット。
- 既存: `get_rows(..., fred_fetcher, ecb_fetcher, boj_fetcher, computed_fetcher, cftc_fetcher)`、`run(..., 同)`、`build_macro_payload(config_path,data_dir,points,calendar)`、index.htmlの`legendHTML/lineChartSVG/chartCaption/renderMoneyFlow`。

---

## File Structure
| パス | 区分 | 責務 |
|---|---|---|
| `fetch_macro.py` | 変更 | MOF定数/キャッシュ、week・介入パーサ、fetcher、mof_dispatch、配線 |
| `tests/test_fetch_macro.py` | 変更 | パーサ・配線テスト |
| `build.py` | 変更 | no_stale |
| `tests/test_build.py` | 変更 | no_stale テスト |
| `series_config.json` | 変更 | 6系列追加（計55） |
| `index.html` | 変更 | マネーの流れに週次フロー比較チャート |

---

## Task 1: week.csv パーサとフェッチャ

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- Produces: `_parse_mof_week_csv(text, kind) -> list[(YYYY-MM-DD, 兆円)]`（kind: flow_out/flow_in/flow_in_eq）、`fetch_mof_flows(kind, urlopen=...)`、定数 `MOF_WEEK_CSV`/`MOF_FEIO_CSV`/`_MOF_CACHE`。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def _mof_week_sample():
    def row(period, c11, c14, c22):
        cells = [period] + ["0"] * 22
        cells[11], cells[14], cells[22] = c11, c14, c22
        return ",".join(f'"{c}"' for c in cells)
    return "\n".join([
        "対外及び対内証券投資契約等の状況,,,",
        "International Transactions,,,",
        row("2026．6．21～6．27", "1,474 ", "-1,332 ", "-47,363 "),
        row("2005．12．26～ 1．1", "10,000", "500", "-2,000"),
    ]) + "\n"


def test_parse_mof_week_csv_kinds_and_rollover():
    text = _mof_week_sample()
    out_total = fetch_macro._parse_mof_week_csv(text, "flow_out")
    in_total = fetch_macro._parse_mof_week_csv(text, "flow_in")
    in_eq = fetch_macro._parse_mof_week_csv(text, "flow_in_eq")
    assert ("2026-06-27", 0.1474) in out_total
    assert ("2026-06-27", -4.7363) in in_total
    assert ("2026-06-27", -0.1332) in in_eq
    # 年跨ぎ: 2005.12.26〜1.1 → 終了日 2006-01-01
    assert ("2006-01-01", 1.0) in out_total
    assert out_total == sorted(out_total)


def test_fetch_mof_flows_uses_cache(monkeypatch):
    fetch_macro._MOF_CACHE.clear()
    calls = {"n": 0}
    body = _mof_week_sample().encode("cp932")
    class _R:
        def read(self): return body
        def __enter__(self): return self
        def __exit__(self, *a): return False
    def fake_urlopen(req, timeout=60):
        calls["n"] += 1
        return _R()
    a = fetch_macro.fetch_mof_flows("flow_out", urlopen=fake_urlopen)
    b = fetch_macro.fetch_mof_flows("flow_in", urlopen=fake_urlopen)
    assert calls["n"] == 1          # 2系列でも取得は1回（キャッシュ）
    assert a and b
    fetch_macro._MOF_CACHE.clear()
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k mof_week -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の定数群（`CFTC_URL = ...` の直後）に追記:

```python
MOF_WEEK_CSV = "https://www.mof.go.jp/policy/international_policy/reference/itn_transactions_in_securities/week.csv"
MOF_FEIO_CSV = "https://www.mof.go.jp/policy/international_policy/reference/feio/foreign_exchange_intervention_operations.csv"
_MOF_CACHE: dict = {}   # URL -> デコード済みテキスト（同一実行内での重複取得を回避）
```

`fetch_cftc` の後ろに追記:

```python
_MOF_WEEK_COLS = {"flow_out": 11, "flow_in": 22, "flow_in_eq": 14}
_MOF_PERIOD_RE = re.compile(r"^(\d{4})\.(\d{1,2})\.(\d{1,2})~(\d{1,2})\.(\d{1,2})$")


def _parse_mof_week_csv(text: str, kind: str) -> list:
    col = _MOF_WEEK_COLS[kind]
    out = []
    for r in csv.reader(io.StringIO(text)):
        if not r or col >= len(r):
            continue
        period = (r[0] or "").replace("．", ".").replace("～", "~")
        period = period.replace(" ", "").replace("　", "")
        m = _MOF_PERIOD_RE.match(period)
        if not m:
            continue
        y, m1, _d1, m2, d2 = (int(x) for x in m.groups())
        if m2 < m1:
            y += 1                     # 年跨ぎ（12月末〜1月）
        val = (r[col] or "").replace(",", "").strip()
        if val in ("", "-"):
            continue
        try:
            v = float(val)
        except ValueError:
            continue
        out.append((f"{y}-{m2:02d}-{d2:02d}", round(v / 10000.0, 4)))   # 億円→兆円
    out.sort(key=lambda x: x[0])
    return out


def _mof_text(url: str, urlopen=urllib.request.urlopen) -> str:
    if url not in _MOF_CACHE:
        req = urllib.request.Request(url, headers={"User-Agent": "Mozilla/5.0"})
        with urlopen(req, timeout=60) as r:
            _MOF_CACHE[url] = r.read().decode("cp932")
    return _MOF_CACHE[url]


def fetch_mof_flows(kind: str, urlopen=urllib.request.urlopen) -> list:
    return _parse_mof_week_csv(_mof_text(MOF_WEEK_CSV, urlopen), kind)
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k mof -v`
Expected: 2件 PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: MOF weekly securities-flow parser (cp932, period-end dates)"
```

---

## Task 2: 介入CSVパーサとフェッチャ

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- Produces: `_parse_mof_intervention(text) -> list[(YYYY-MM-01, 兆円)]`、`fetch_mof_intervention(urlopen=...)`。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_parse_mof_intervention_carryover_and_monthly_sum():
    rows = [
        "財務省,,,,,,,,",
        "平成3年,5月,13日,1991,May,13,139,米ドル買・円売,x",
        ",6月,10日,,Jun,10,211,米ドル買・円売,x",
        ",,13日,,,13,213,米ドル買・円売,x",
        "平成3年4〜6月合計,,,April - June 1991,,,563,,",
        "平成4年,8月,19日,1992,Aug,19,100,米ドル買・円売,x",
    ]
    out = fetch_macro._parse_mof_intervention("\n".join(rows) + "\n")
    assert ("1991-05-01", 0.0139) in out
    assert ("1991-06-01", 0.0424) in out      # 211+213=424億円
    assert ("1992-08-01", 0.01) in out
    assert all("合計" not in d for d, _ in out)
    assert out == sorted(out)
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k intervention -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_mof_flows` の後ろに追記:

```python
_MOF_MONTHS = {"Jan": 1, "Feb": 2, "Mar": 3, "Apr": 4, "May": 5, "Jun": 6,
               "Jul": 7, "Aug": 8, "Sep": 9, "Oct": 10, "Nov": 11, "Dec": 12}


def _parse_mof_intervention(text: str) -> list:
    totals: dict = {}
    year = month = None
    for r in csv.reader(io.StringIO(text)):
        if len(r) < 7:
            continue
        y = (r[3] or "").strip()
        mo = (r[4] or "").strip()
        d = (r[5] or "").strip()
        amt = (r[6] or "").replace(",", "").strip()
        if y.isdigit():
            year = int(y)
        if mo in _MOF_MONTHS:
            month = _MOF_MONTHS[mo]
        if not d.isdigit() or year is None or month is None:
            continue                     # 合計行（日が空）や見出しをスキップ
        try:
            v = float(amt)
        except ValueError:
            continue
        key = f"{year}-{month:02d}-01"
        totals[key] = totals.get(key, 0.0) + v
    return sorted((k, round(v / 10000.0, 4)) for k, v in totals.items())


def fetch_mof_intervention(urlopen=urllib.request.urlopen) -> list:
    return _parse_mof_intervention(_mof_text(MOF_FEIO_CSV, urlopen))
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k intervention -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: MOF FX intervention parser (monthly totals)"
```

---

## Task 3: mof ソース配線（mof_dispatch / get_rows / run）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- `mof_dispatch(series, urlopen=urllib.request.urlopen) -> list`（`mof_kind`: intervention→介入、他→flows）
- `get_rows(..., mof_fetcher=None)`、`run(..., mof_fetcher=None, ...)`。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_run_handles_mof_source(tmp_path):
    cfg = {"series": [{"id": "MOF_FLOW_IN", "source": "mof",
                       "mof_kind": "flow_in", "transform": "level"}]}
    def fake_mof(series):
        assert series["mof_kind"] == "flow_in"
        return [("2026-06-20", -1.2), ("2026-06-27", -4.7363)]
    res = fetch_macro.run(cfg, "key", mof_fetcher=fake_mof, data_dir=tmp_path)
    assert res["ok"] == ["MOF_FLOW_IN"]
    txt = (tmp_path / "MOF_FLOW_IN.csv").read_text(encoding="utf-8")
    assert "-4.7363" in txt
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k mof_source -v`
Expected: FAIL（run が mof_fetcher 未対応）

- [ ] **Step 3: 実装を変更**

`fetch_mof_intervention` の後ろに追記:

```python
def mof_dispatch(series: dict, urlopen=urllib.request.urlopen) -> list:
    kind = series.get("mof_kind")
    if kind == "intervention":
        return fetch_mof_intervention(urlopen)
    return fetch_mof_flows(kind, urlopen)
```

`get_rows` を次に置換:

```python
def get_rows(series: dict, api_key: str, *, fred_fetcher, ecb_fetcher, boj_fetcher=None, computed_fetcher=None, cftc_fetcher=None, mof_fetcher=None) -> list:
    src = series.get("source")
    if src == "ecb":
        return ecb_fetcher(series["ecb_key"])
    if src == "boj":
        return boj_fetcher()
    if src == "cftc":
        return (cftc_fetcher or fetch_cftc)(series["cftc_market"])
    if src == "mof":
        return (mof_fetcher or mof_dispatch)(series)
    if src == "computed":
        return (computed_fetcher or computed_dispatch)(series, api_key, fred_fetcher)
    return parse_observations(fred_fetcher(series["id"], api_key))
```

`run` のシグネチャに `mof_fetcher=None` を追加（`cftc_fetcher=None,` の後）:

```python
def run(config: dict, api_key: str, fetcher=fetch_series, ecb_fetcher=None, boj_fetcher=None, computed_fetcher=None, cftc_fetcher=None, mof_fetcher=None, data_dir: Path = MACRO_DIR) -> dict:
```

`run` 内の `get_rows(...)` 呼び出しを次に置換:

```python
            rows = get_rows(s, api_key, fred_fetcher=fetcher, ecb_fetcher=ecb_fetcher, boj_fetcher=boj_fetcher, computed_fetcher=computed_fetcher, cftc_fetcher=cftc_fetcher, mof_fetcher=mof_fetcher)
```

- [ ] **Step 4: 全テスト確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: wire mof source into dispatch and run"
```

---

## Task 4: build に no_stale

**Files:** Modify `build.py`, `tests/test_build.py`

**Interfaces:** config の `"no_stale": true` 系列は payload で `stale=False, stale_days=0`。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` 末尾に追記:

```python
def test_no_stale_flag_suppresses_staleness(tmp_path):
    (tmp_path / "macro_data").mkdir()
    (tmp_path / "macro_data" / "OLD.csv").write_text(
        "date,value\n2020-01-01,1\n2020-02-01,2\n", encoding="utf-8")
    (tmp_path / "series_config.json").write_text(json.dumps({
        "history_points": 180, "series": [
            {"id": "OLD", "country": "c", "indicator": "i", "unit": "u",
             "transform": "level", "no_stale": True}]}), encoding="utf-8")
    p = build.build_macro_payload(
        config_path=tmp_path / "series_config.json", data_dir=tmp_path / "macro_data")
    s = p["series"][0]
    assert s["stale"] is False and s["stale_days"] == 0
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_build.py -k no_stale -v`
Expected: FAIL（staleがTrue）

- [ ] **Step 3: 実装を変更**

`build.py` の `build_macro_payload` 内、`stale_days, stale = _staleness(rows, date.today())` の直後に追記:

```python
        if s.get("no_stale"):
            stale_days, stale = 0, False
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add build.py tests/test_build.py
git commit -m "feat: no_stale flag for event-type series"
```

---

## Task 5: 設定6系列＋実データ取得

**Files:** Modify `series_config.json`

- [ ] **Step 1: series_config.json に追記**

`SPREAD_FF_ECB` の行の直後に追記:

```json
    { "id": "MOF_FLOW_OUT", "country": "日本勢の対外投資ネット", "indicator": "国際資金フロー", "unit": "兆円/週", "transform": "level", "source": "mof", "mof_kind": "flow_out", "group": "market" },
    { "id": "MOF_FLOW_IN", "country": "海外勢の対日投資ネット", "indicator": "国際資金フロー", "unit": "兆円/週", "transform": "level", "source": "mof", "mof_kind": "flow_in", "group": "market" },
    { "id": "MOF_FLOW_IN_EQ", "country": "海外勢の日本株ネット", "indicator": "国際資金フロー", "unit": "兆円/週", "transform": "level", "source": "mof", "mof_kind": "flow_in_eq", "group": "market" },
    { "id": "MOF_INTERVENTION", "country": "為替介入額(実施月のみ)", "indicator": "為替介入", "unit": "兆円/月", "transform": "level", "source": "mof", "mof_kind": "intervention", "group": "market", "no_stale": true },
    { "id": "WMTSECL1", "country": "海外公的部門の米国債保有", "indicator": "国際資金フロー", "unit": "百万ドル", "transform": "level", "group": "market" },
    { "id": "FDHBFIN", "country": "海外勢の米国債保有(TIC)", "indicator": "国際資金フロー", "unit": "百万ドル", "transform": "level", "group": "market", "no_stale": true },
```

- [ ] **Step 2: JSON妥当性**

Run: `python -c "import json; print(len(json.load(open('series_config.json',encoding='utf-8'))['series']),'series')"`
Expected: `55 series`

- [ ] **Step 3: 実データ取得と確認**

Run: `python fetch_macro.py`
Expected: `OK: 55 series 取得`。
Run: `python -c "import build; p=build.build_macro_payload(); xs=[s for s in p['series'] if s['indicator'] in ('国際資金フロー','為替介入')]; [print(s['country'], s['latest_date'], s['latest'], 'z', s['zscore'], 'stale', s['stale']) for s in xs]"`
Expected: MOF3系列（最新 2026-06-27週）・介入（実施月のみ）・TIC2系列。介入とFDHBFINは stale False。

- [ ] **Step 4: ビルドとCommit**

Run: `python build.py`

```bash
git add series_config.json macro_data data.js "週次ブリーフィング.md"
git commit -m "data: international capital flows (MOF weekly, intervention, TIC)"
```

---

## Task 6: マネーの流れに週次フロー比較チャート＋最終検証

**Files:** Modify `index.html`

- [ ] **Step 1: renderMoneyFlow にフロー比較カードを追加**

`renderMoneyFlow` 内、`const analysisCard = ...` の**直前**（glCard定義の後）に追記:

```javascript
  const fo = macro.find(s => s.id === 'MOF_FLOW_OUT');
  const fi = macro.find(s => s.id === 'MOF_FLOW_IN');
  const flowLines = [fo, fi].filter(Boolean).map((s, i) => ({label: s.country, points: s.history || [], color: ['#4f8cff', '#ff8c4f'][i]}));
  const flowCard = flowLines.length ? `
    <div class="card">
      <h3>対外・対内証券フロー（週次ネット・兆円）</h3>
      ${legendHTML(flowLines)}
      ${lineChartSVG(flowLines, {h:140})}
      ${chartCaption(flowLines)}
      <div class="meta-row">＋＝取得超（対外＝日本勢の外向き投資、対内＝海外勢の対日投資）。出典: 財務省（週次・指定報告機関ベース）</div>
    </div>` : '';
```

同関数内の `el.innerHTML = \`` 直後の `${glCard}` を `${glCard}${flowCard}` に置換。

- [ ] **Step 2: ブラウザ確認**

`g7-static` 起動、再読込。
Expected:
- 「マネーの流れ」: 世界流動性の下に「対外・対内証券フロー（週次ネット・兆円）」2本線＋凡例＋出典行。
- 「市場・リスク」: テーマ「国際資金フロー」（5カード）と「為替介入」（1カード）。介入・TIC(四半期)に⚠鮮度警告が**出ない**。
- コンソールエラーなし。

- [ ] **Step 3: 全テスト確認とCommit＆push**

Run: `python -m pytest -q`
Expected: 全PASS。

```bash
git add index.html
git commit -m "feat: weekly cross-border flow chart on money-flow view"
git push
```

---

## 完了の定義
- [ ] 55系列取得。「国際資金フロー」「為替介入」テーマ表示。
- [ ] マネーの流れに週次フロー比較チャート（対外/対内）。
- [ ] 介入・TIC四半期に鮮度警告なし（no_stale）。
- [ ] `python -m pytest` 全PASS。push後、CI/Pagesに自動反映。
