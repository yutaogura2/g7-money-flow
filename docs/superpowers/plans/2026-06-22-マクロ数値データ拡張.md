# マクロ数値データ拡張 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 既存G7ツールに、FREDから機械取得するマクロ数値（マネーサプライ・中銀BS・金利・物価・景気雇用）の時系列レイヤーを追加し、ダッシュボードの「マクロ」「マネーの流れ」ビュー・Excelシート・Git履歴で正確に蓄積・分析できるようにする。

**Architecture:** `series_config.json` で定義したFREDシリーズを `fetch_macro.py` が取得して `macro_data/<id>.csv`（Git追跡）に保存。`build.py` がCSVと `data.json` を統合し `data.js`（チャート用）とExcel（マクロ2シート）を生成。`index.html` は依存ライブラリなしのインラインSVGでチャートを描く。

**Tech Stack:** Python 3.12（標準ライブラリ urllib・csv、既存 openpyxl）、Vanilla HTML/CSS/JS（SVGチャート自前）、pytest。**新規依存なし。**

## Global Constraints

- 配置ルート: `C:\Users\yutao\OneDrive\デスクトップ\★Claud code★\銀行一次情報`（パスは相対）。
- 文字コードは全てUTF-8。Python入出力は `encoding="utf-8"`。
- 数値の真実の源は `macro_data/*.csv`、質的情報は `data.json`。生成物（`data.js`・xlsx）は手編集しない。
- FRED APIキーは `fred_api_key.txt`（1行）に保存し、**`.gitignore` で除外＝コミットしない**。
- FRED取得エンドポイント: `https://api.stlouisfed.org/fred/series/observations`、`file_type=json`。
- 取得失敗・欠損シリーズは**スキップして明示**（推測で埋めない）。全体は止めない。
- `index.html` は `file://` でオフライン動作を維持（外部fetch・外部ライブラリ禁止）。
- 既存テスト（`tests/test_build.py`）を壊さない。Gitは初期化済み（ブランチ `main`）。各タスク末尾でコミット。
- 既存のグローバル定数: `build.ROOT/DATA_JSON/DATA_JS/XLSX/MEETING_HEADERS/CALENDAR_HEADERS/ANALYSIS_HEADERS/ASPECTS`、関数 `load_data/write_data_js/build_xlsx/_style_header/main`。`index.html` のグローバル: `DATA/esc/byDateDesc/todayStr/switchView/init` と各 `render*`。

---

## File Structure

| パス | 区分 | 責務 |
|---|---|---|
| `series_config.json` | 新規 | FREDシリーズ定義（国・指標・id・単位・transform）＋ history_start/points |
| `fetch_macro.py` | 新規 | FRED取得→`macro_data/*.csv` 生成（設定/キー読込・取得・YoY計算・CSV書込・統括） |
| `macro_data/<id>.csv` | 新規(生成) | シリーズ別時系列 `date,value[,yoy_pct]`。Git追跡 |
| `fred_api_key.txt` | 新規 | FRED APIキー。`.gitignore` で除外 |
| `tests/test_fetch_macro.py` | 新規 | fetch_macro のテスト |
| `build.py` | 変更 | macroペイロードを data.js に注入、Excelにマクロ2シート追加 |
| `index.html` | 変更 | 「マクロ」「マネーの流れ」ビュー＋SVGチャート |
| `.gitignore` | 変更 | `fred_api_key.txt` 追加 |
| `更新手順.md` | 変更 | マクロ更新手順・FREDキー取得方法 |
| `data.json` | 変更 | `macro_analysis` フィールド追加（考察ナラティブ） |

---

## Task 1: 設定・APIキー読込と .gitignore

**Files:**
- Create: `series_config.json`, `fetch_macro.py`
- Create: `tests/test_fetch_macro.py`
- Modify: `.gitignore`

**Interfaces:**
- Produces:
  - `fetch_macro.load_config(path="series_config.json") -> dict`（`{"history_start":str,"history_points":int,"series":[{id,country,indicator,unit,transform}]}`）
  - `fetch_macro.load_api_key(path="fred_api_key.txt") -> str`（無ければ `SystemExit` で案内）

- [ ] **Step 1: `.gitignore` に APIキーを追加**

`.gitignore` の末尾に追記:

```
# FRED APIキー（秘密：コミットしない）
fred_api_key.txt
```

- [ ] **Step 2: `series_config.json` を作成（starter。各idは後で存在確認）**

```json
{
  "history_start": "2000-01-01",
  "history_points": 180,
  "series": [
    { "id": "M2SL", "country": "米国", "indicator": "マネーサプライ(M2)", "unit": "10億ドル", "transform": "yoy_pct_also" },
    { "id": "WALCL", "country": "米国", "indicator": "中銀バランスシート", "unit": "百万ドル", "transform": "level" },
    { "id": "FEDFUNDS", "country": "米国", "indicator": "政策金利", "unit": "%", "transform": "level" },
    { "id": "DGS10", "country": "米国", "indicator": "10年国債利回り", "unit": "%", "transform": "level" },
    { "id": "CPIAUCSL", "country": "米国", "indicator": "CPI", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "CPILFESL", "country": "米国", "indicator": "コアCPI", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "GDPC1", "country": "米国", "indicator": "実質GDP", "unit": "10億ドル", "transform": "yoy_pct_also" },
    { "id": "UNRATE", "country": "米国", "indicator": "失業率", "unit": "%", "transform": "level" },
    { "id": "MYAGM2EZM196N", "country": "ユーロ圏", "indicator": "マネーサプライ(M2)", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "ECBASSETSW", "country": "ユーロ圏", "indicator": "中銀バランスシート", "unit": "百万ユーロ", "transform": "level" },
    { "id": "ECBDFR", "country": "ユーロ圏", "indicator": "政策金利", "unit": "%", "transform": "level" },
    { "id": "IRLTLT01DEM156N", "country": "ユーロ圏", "indicator": "10年国債利回り", "unit": "%", "transform": "level" },
    { "id": "CP0000EZ19M086NEST", "country": "ユーロ圏", "indicator": "CPI", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "MYAGM2JPM189S", "country": "日本", "indicator": "マネーサプライ(M2)", "unit": "円", "transform": "yoy_pct_also" },
    { "id": "JPNASSETS", "country": "日本", "indicator": "中銀バランスシート", "unit": "億円", "transform": "level" },
    { "id": "IRLTLT01JPM156N", "country": "日本", "indicator": "10年国債利回り", "unit": "%", "transform": "level" },
    { "id": "JPNCPIALLMINMEI", "country": "日本", "indicator": "CPI", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "LRUNTTTTJPM156S", "country": "日本", "indicator": "失業率", "unit": "%", "transform": "level" },
    { "id": "MABMM201GBM189S", "country": "英国", "indicator": "マネーサプライ(M2)", "unit": "ポンド", "transform": "yoy_pct_also" },
    { "id": "IRLTLT01GBM156N", "country": "英国", "indicator": "10年国債利回り", "unit": "%", "transform": "level" },
    { "id": "GBRCPIALLMINMEI", "country": "英国", "indicator": "CPI", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "MYAGM2CNM189N", "country": "中国", "indicator": "マネーサプライ(M2)", "unit": "元", "transform": "yoy_pct_also" },
    { "id": "CHNCPIALLMINMEI", "country": "中国", "indicator": "CPI", "unit": "指数", "transform": "yoy_pct_also" },
    { "id": "IRLTLT01CAM156N", "country": "カナダ", "indicator": "10年国債利回り", "unit": "%", "transform": "level" },
    { "id": "CANCPIALLMINMEI", "country": "カナダ", "indicator": "CPI", "unit": "指数", "transform": "yoy_pct_also" }
  ]
}
```

- [ ] **Step 3: 失敗するテストを書く**

`tests/test_fetch_macro.py`:

```python
import json
from pathlib import Path

import pytest

import fetch_macro


def test_load_config(tmp_path):
    p = tmp_path / "series_config.json"
    p.write_text(json.dumps({"history_points": 180, "series": [{"id": "M2SL"}]}), encoding="utf-8")
    cfg = fetch_macro.load_config(p)
    assert cfg["history_points"] == 180
    assert cfg["series"][0]["id"] == "M2SL"


def test_load_api_key_reads_and_strips(tmp_path):
    p = tmp_path / "fred_api_key.txt"
    p.write_text("  abcd1234\n", encoding="utf-8")
    assert fetch_macro.load_api_key(p) == "abcd1234"


def test_load_api_key_missing_raises(tmp_path):
    with pytest.raises(SystemExit):
        fetch_macro.load_api_key(tmp_path / "nope.txt")
```

- [ ] **Step 4: テスト失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: FAIL（`No module named 'fetch_macro'`）

- [ ] **Step 5: `fetch_macro.py` を作成（設定・キー読込まで）**

`fetch_macro.py`:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""FRED からマクロ時系列を取得し macro_data/*.csv を生成する。"""
import json
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent
SERIES_CONFIG = ROOT / "series_config.json"
API_KEY_FILE = ROOT / "fred_api_key.txt"
MACRO_DIR = ROOT / "macro_data"
FRED_URL = "https://api.stlouisfed.org/fred/series/observations"


def load_config(path: Path = SERIES_CONFIG) -> dict:
    with open(path, encoding="utf-8") as f:
        return json.load(f)


def load_api_key(path: Path = API_KEY_FILE) -> str:
    p = Path(path)
    if not p.exists():
        raise SystemExit(
            f"FRED APIキーがありません。{p.name} に無料キーを1行で保存してください "
            f"(取得: https://fred.stlouisfed.org/docs/api/api_key.html)"
        )
    return p.read_text(encoding="utf-8").strip()
```

- [ ] **Step 6: テスト成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: PASS（3件）

- [ ] **Step 7: Commit**

```bash
git add .gitignore series_config.json fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: macro config and FRED api-key loading"
```

---

## Task 2: FRED観測値のパース・YoY計算・CSV書き出し

**Files:**
- Modify: `fetch_macro.py`
- Modify: `tests/test_fetch_macro.py`

**Interfaces:**
- Produces:
  - `parse_observations(payload: dict) -> list[tuple[str, float]]`（日付昇順、欠損 `.` を除外）
  - `compute_yoy(rows: list[tuple[str,float]]) -> list[tuple[str, float, float|None]]`（1年前との比較%）
  - `to_csv_text(rows, with_yoy: bool) -> str`
  - `write_series_csv(series_id: str, rows, with_yoy: bool, data_dir=MACRO_DIR) -> Path`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` の末尾に追記:

```python
def test_parse_observations_filters_missing_and_sorts():
    payload = {"observations": [
        {"date": "2020-03-01", "value": "3.0"},
        {"date": "2020-01-01", "value": "1.0"},
        {"date": "2020-02-01", "value": "."},
    ]}
    rows = fetch_macro.parse_observations(payload)
    assert rows == [("2020-01-01", 1.0), ("2020-03-01", 3.0)]


def test_compute_yoy_matches_one_year_prior():
    rows = [(f"20{y:02d}-01-01", float(v)) for y, v in
            [(20, 100.0), (21, 110.0), (22, 121.0)]]
    out = fetch_macro.compute_yoy(rows)
    assert out[0] == ("2020-01-01", 100.0, None)   # 1年前なし
    assert out[1] == ("2021-01-01", 110.0, 10.0)   # +10%
    assert out[2] == ("2022-01-01", 121.0, 10.0)   # +10%


def test_to_csv_text_with_and_without_yoy():
    assert fetch_macro.to_csv_text([("2020-01-01", 1.0)], False) == "date,value\n2020-01-01,1.0\n"
    txt = fetch_macro.to_csv_text([("2020-01-01", 1.0, None), ("2021-01-01", 1.1, 10.0)], True)
    assert txt == "date,value,yoy_pct\n2020-01-01,1.0,\n2021-01-01,1.1,10.0\n"


def test_write_series_csv(tmp_path):
    fp = fetch_macro.write_series_csv("X", [("2020-01-01", 1.0)], False, data_dir=tmp_path)
    assert fp.exists()
    assert fp.read_text(encoding="utf-8") == "date,value\n2020-01-01,1.0\n"
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k "parse or yoy or csv" -v`
Expected: FAIL（関数未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の import 群に追記:

```python
from datetime import date
```

`load_api_key` の後ろに追記:

```python
def parse_observations(payload: dict) -> list:
    out = []
    for o in payload.get("observations", []):
        val = o.get("value", ".")
        if val in (".", "", None):
            continue
        try:
            out.append((o["date"], float(val)))
        except (ValueError, KeyError):
            continue
    out.sort(key=lambda r: r[0])
    return out


def _prev_year(d: str) -> str:
    y, m, dd = (int(x) for x in d.split("-"))
    try:
        return date(y - 1, m, dd).isoformat()
    except ValueError:        # 2月29日など
        return date(y - 1, m, 28).isoformat()


def compute_yoy(rows: list) -> list:
    out = []
    for i, (d, v) in enumerate(rows):
        target = _prev_year(d)
        prev = None
        for dj, vj in rows[:i]:
            if dj <= target:
                prev = vj
            else:
                break
        yoy = round((v / prev - 1) * 100, 2) if prev not in (None, 0) else None
        out.append((d, v, yoy))
    return out


def to_csv_text(rows: list, with_yoy: bool) -> str:
    if with_yoy:
        lines = ["date,value,yoy_pct"]
        for d, v, y in rows:
            lines.append(f"{d},{v},{'' if y is None else y}")
    else:
        lines = ["date,value"]
        for d, v in rows:
            lines.append(f"{d},{v}")
    return "\n".join(lines) + "\n"


def write_series_csv(series_id: str, rows: list, with_yoy: bool, data_dir: Path = MACRO_DIR) -> Path:
    p = Path(data_dir)
    p.mkdir(parents=True, exist_ok=True)
    fp = p / f"{series_id}.csv"
    fp.write_text(to_csv_text(rows, with_yoy), encoding="utf-8")
    return fp
```

- [ ] **Step 4: テスト成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: FRED parse, YoY, and CSV writing"
```

---

## Task 3: 取得の統括（run/fetch_series/main、欠損スキップ）

**Files:**
- Modify: `fetch_macro.py`
- Modify: `tests/test_fetch_macro.py`

**Interfaces:**
- Consumes: `load_config/load_api_key/parse_observations/compute_yoy/write_series_csv`
- Produces:
  - `fetch_series(series_id: str, api_key: str, urlopen=urllib.request.urlopen) -> dict`
  - `run(config: dict, api_key: str, fetcher=fetch_series, data_dir=MACRO_DIR) -> dict`（`{"ok":[id...], "failed":[(id,err)...]}`）
  - `main() -> int`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` の末尾に追記:

```python
def test_run_writes_ok_and_skips_failures(tmp_path):
    cfg = {"series": [
        {"id": "GOOD", "transform": "yoy_pct_also"},
        {"id": "BAD", "transform": "level"},
        {"id": "EMPTY", "transform": "level"},
    ]}

    def fake_fetcher(sid, key):
        if sid == "GOOD":
            return {"observations": [
                {"date": "2020-01-01", "value": "100"},
                {"date": "2021-01-01", "value": "110"},
            ]}
        if sid == "EMPTY":
            return {"observations": []}
        raise RuntimeError("boom")

    res = fetch_macro.run(cfg, "key", fetcher=fake_fetcher, data_dir=tmp_path)
    assert res["ok"] == ["GOOD"]
    failed_ids = [f[0] for f in res["failed"]]
    assert "BAD" in failed_ids and "EMPTY" in failed_ids
    good = (tmp_path / "GOOD.csv").read_text(encoding="utf-8")
    assert good.startswith("date,value,yoy_pct")
    assert "10.0" in good  # YoY が計算されている
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k run -v`
Expected: FAIL（`run` 未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の import 群に追記:

```python
import urllib.parse
import urllib.request
```

`write_series_csv` の後ろに追記:

```python
def fetch_series(series_id: str, api_key: str, urlopen=urllib.request.urlopen) -> dict:
    q = urllib.parse.urlencode({"series_id": series_id, "api_key": api_key, "file_type": "json"})
    with urlopen(f"{FRED_URL}?{q}", timeout=30) as r:
        return json.loads(r.read().decode("utf-8"))


def run(config: dict, api_key: str, fetcher=fetch_series, data_dir: Path = MACRO_DIR) -> dict:
    ok, failed = [], []
    for s in config.get("series", []):
        sid = s["id"]
        with_yoy = s.get("transform") == "yoy_pct_also"
        try:
            rows = parse_observations(fetcher(sid, api_key))
            if not rows:
                failed.append((sid, "観測値なし"))
                continue
            out = compute_yoy(rows) if with_yoy else rows
            write_series_csv(sid, out, with_yoy, data_dir)
            ok.append(sid)
        except Exception as e:  # noqa: BLE001  個別失敗はスキップして継続
            failed.append((sid, str(e)))
    return {"ok": ok, "failed": failed}


def main() -> int:
    config = load_config()
    api_key = load_api_key()
    res = run(config, api_key)
    print(f"OK: {len(res['ok'])} series 取得")
    for sid, err in res["failed"]:
        print(f"  未取得 {sid}: {err}", file=sys.stderr)
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: テスト成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: fetch orchestration with graceful skip"
```

---

## Task 4: build.py — macroペイロードを data.js に注入

**Files:**
- Modify: `build.py`
- Modify: `tests/test_build.py`

**Interfaces:**
- Consumes: `macro_data/*.csv`, `series_config.json`
- Produces:
  - `build._read_series_csv(path) -> list[tuple[str,float,float|None]]`
  - `build.build_macro_payload(config_path=None, data_dir=None, points=None) -> dict`（None時はモジュール定数を呼び出し時に解決。`{"series":[{id,country,indicator,unit,latest,latest_date,yoy,history,history_yoy}]}`）
  - `build.macro_timeseries_rows(config_path=None, data_dir=None) -> list[tuple]`（`(date,country,indicator,value,yoy)`、history_start以降）
  - 定数 `build.SERIES_CONFIG`, `build.MACRO_DIR`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` の末尾に追記:

```python
def _write_macro_fixture(tmp_path):
    (tmp_path / "macro_data").mkdir()
    (tmp_path / "macro_data" / "M2SL.csv").write_text(
        "date,value,yoy_pct\n2019-01-01,100.0,\n2020-01-01,110.0,10.0\n", encoding="utf-8")
    (tmp_path / "series_config.json").write_text(
        json.dumps({"history_start": "2000-01-01", "history_points": 180,
                    "series": [{"id": "M2SL", "country": "米国",
                                "indicator": "マネーサプライ(M2)", "unit": "10億ドル",
                                "transform": "yoy_pct_also"}]}), encoding="utf-8")


def test_build_macro_payload(tmp_path):
    _write_macro_fixture(tmp_path)
    payload = build.build_macro_payload(
        config_path=tmp_path / "series_config.json", data_dir=tmp_path / "macro_data")
    s = payload["series"][0]
    assert s["country"] == "米国"
    assert s["latest"] == 110.0
    assert s["latest_date"] == "2020-01-01"
    assert s["yoy"] == 10.0
    assert s["history"] == [["2019-01-01", 100.0], ["2020-01-01", 110.0]]
    assert s["history_yoy"] == [["2020-01-01", 10.0]]


def test_macro_timeseries_rows(tmp_path):
    _write_macro_fixture(tmp_path)
    rows = build.macro_timeseries_rows(
        config_path=tmp_path / "series_config.json", data_dir=tmp_path / "macro_data")
    assert ("2020-01-01", "米国", "マネーサプライ(M2)", 110.0, 10.0) in rows
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_build.py -k macro -v`
Expected: FAIL（関数未定義）

- [ ] **Step 3: build.py に実装を追加**

`build.py` の import 群に追記:

```python
import csv
```

`ROOT = Path(__file__).resolve().parent` の直後に追記:

```python
SERIES_CONFIG = ROOT / "series_config.json"
MACRO_DIR = ROOT / "macro_data"
```

`write_data_js` の後ろ（`MEETING_HEADERS` の前）に追記:

```python
def _read_series_csv(path: Path) -> list:
    rows = []
    with open(path, encoding="utf-8", newline="") as f:
        reader = csv.reader(f)
        header = next(reader, None)
        has_yoy = header is not None and "yoy_pct" in header
        for line in reader:
            if len(line) < 2 or line[1] == "":
                continue
            d = line[0]
            v = float(line[1])
            y = float(line[2]) if (has_yoy and len(line) > 2 and line[2] != "") else None
            rows.append((d, v, y))
    return rows


def build_macro_payload(config_path=None, data_dir=None, points=None) -> dict:
    config_path = config_path or SERIES_CONFIG      # 呼び出し時に解決（monkeypatch対応）
    data_dir = data_dir or MACRO_DIR
    if not Path(config_path).exists():
        return {"series": []}
    cfg = json.loads(Path(config_path).read_text(encoding="utf-8"))
    pts = points or cfg.get("history_points", 180)
    series = []
    for s in cfg.get("series", []):
        fp = Path(data_dir) / f"{s['id']}.csv"
        if not fp.exists():
            continue
        rows = _read_series_csv(fp)
        if not rows:
            continue
        d, v, y = rows[-1]
        series.append({
            "id": s["id"], "country": s["country"], "indicator": s["indicator"],
            "unit": s.get("unit", ""),
            "latest": v, "latest_date": d, "yoy": y,
            "history": [[r[0], r[1]] for r in rows][-pts:],
            "history_yoy": [[r[0], r[2]] for r in rows if r[2] is not None][-pts:],
        })
    return {"series": series}


def macro_timeseries_rows(config_path=None, data_dir=None) -> list:
    config_path = config_path or SERIES_CONFIG      # 呼び出し時に解決（monkeypatch対応）
    data_dir = data_dir or MACRO_DIR
    if not Path(config_path).exists():
        return []
    cfg = json.loads(Path(config_path).read_text(encoding="utf-8"))
    start = cfg.get("history_start", "2000-01-01")
    out = []
    for s in cfg.get("series", []):
        fp = Path(data_dir) / f"{s['id']}.csv"
        if not fp.exists():
            continue
        for d, v, y in _read_series_csv(fp):
            if d >= start:
                out.append((d, s["country"], s["indicator"], v, y))
    out.sort(key=lambda r: (r[0], r[1], r[2]))
    return out
```

`main()` の `data = load_data()` の直後に追記:

```python
    data["macro"] = build_macro_payload()
```

- [ ] **Step 4: テスト成功を確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全PASS（既存＋macro 2件）

- [ ] **Step 5: Commit**

```bash
git add build.py tests/test_build.py
git commit -m "feat: build macro payload into data.js"
```

---

## Task 5: build.py — Excel マクロ2シート追加

**Files:**
- Modify: `build.py`
- Modify: `tests/test_build.py`

**Interfaces:**
- Consumes: `build_macro_payload`（`data["macro"]`）, `macro_timeseries_rows`
- Produces: `build_xlsx` がシート `マクロ_最新`・`マクロ_時系列` を追加生成

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` の末尾に追記:

```python
def test_build_xlsx_includes_macro_sheets(tmp_path, sample_json, monkeypatch):
    _write_macro_fixture(tmp_path)
    monkeypatch.setattr(build, "SERIES_CONFIG", tmp_path / "series_config.json")
    monkeypatch.setattr(build, "MACRO_DIR", tmp_path / "macro_data")
    data = build.load_data(sample_json)
    data["macro"] = build.build_macro_payload(
        config_path=tmp_path / "series_config.json", data_dir=tmp_path / "macro_data")
    out = tmp_path / "out.xlsx"
    build.build_xlsx(data, out)
    wb = load_workbook(out)
    assert "マクロ_最新" in wb.sheetnames
    assert "マクロ_時系列" in wb.sheetnames
    assert wb["マクロ_最新"].cell(row=1, column=1).value == "国"
    assert wb["マクロ_最新"].max_row == 2          # ヘッダ + 1シリーズ
    assert wb["マクロ_時系列"].max_row == 3         # ヘッダ + 2行
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_build.py -k macro_sheets -v`
Expected: FAIL（シートが無い）

- [ ] **Step 3: build.py の `build_xlsx` にマクロシートを追加**

`build_xlsx` 内、`_style_header(ws3)`（考察サマリ）の後ろ・列幅ループ（`for ws in wb.worksheets:`）の前に追記:

```python
    macro = data.get("macro", {}).get("series", [])
    ws4 = wb.create_sheet("マクロ_最新")
    ws4.append(["国", "指標", "最新値", "日付", "前年比%", "単位"])
    for s in macro:
        ws4.append([s["country"], s["indicator"], s["latest"], s["latest_date"],
                    "" if s.get("yoy") is None else s["yoy"], s.get("unit", "")])
    _style_header(ws4)

    ws5 = wb.create_sheet("マクロ_時系列")
    ws5.append(["日付", "国", "指標", "値", "前年比%"])
    for d, country, ind, v, y in macro_timeseries_rows():
        ws5.append([d, country, ind, v, "" if y is None else y])
    _style_header(ws5)
```

- [ ] **Step 4: テスト成功を確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全PASS

- [ ] **Step 5: 既存データで再ビルド確認（macro未取得でも壊れない）**

Run: `python build.py`
Expected: `OK: 7 meetings, 19 calendar events`。`macro_data` がまだ無くてもエラーにならず、`マクロ_最新`/`マクロ_時系列` は空（ヘッダのみ）で生成される。

- [ ] **Step 6: Commit**

```bash
git add build.py tests/test_build.py
git commit -m "feat: add macro sheets to Excel"
```

---

## Task 6: index.html — SVGチャート＋「マクロ」ビュー

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `DATA.macro.series`, `esc`
- Produces: `lineChartSVG(seriesList, opts)`, `indexTo100(points)`, `macroCard(s)`, `renderMacro()`。nav・section・`switchView`配列・`init` を更新。

- [ ] **Step 1: nav にボタン2つを追加**

`index.html` の `<nav id="tabs">` 内、`<button data-view="analysis">考察ビュー</button>` の後ろに追記:

```html
  <button data-view="macro">マクロ</button>
  <button data-view="moneyflow">マネーの流れ</button>
```

- [ ] **Step 2: section を2つ追加**

`<main>` 内、`<section id="view-analysis" class="view hidden"></section>` の後ろに追記:

```html
  <section id="view-macro" class="view hidden"></section>
  <section id="view-moneyflow" class="view hidden"></section>
```

- [ ] **Step 3: `switchView` の配列に2ビューを追加**

`switchView` 内の配列を次に置換:

```javascript
  ['overview','meetings','calendar','analysis','macro','moneyflow'].forEach(v =>
    document.getElementById('view-'+v).classList.toggle('hidden', v !== name));
```

- [ ] **Step 4: チャート関数とマクロビューを実装**

`renderAnalysis` 関数の後ろ（`function init(){` の前）に追記:

```javascript
function indexTo100(points){
  if(!points.length) return [];
  const base = points[0][1];
  if(!base) return points;
  return points.map(p => [p[0], (p[1]/base)*100]);
}

function lineChartSVG(seriesList, opts){
  opts = opts || {};
  const W = opts.w || 320, H = opts.h || 90, pad = 4;
  const vals = [], xset = new Set();
  seriesList.forEach(s => (s.points||[]).forEach(p => { vals.push(p[1]); xset.add(p[0]); }));
  if(!vals.length) return '<div class="meta-row">データなし</div>';
  const xs = [...xset].sort();
  const nx = Math.max(1, xs.length - 1);
  const minV = Math.min(...vals), maxV = Math.max(...vals);
  const sx = d => pad + (xs.indexOf(d)/nx) * (W - 2*pad);
  const sy = v => (maxV===minV) ? H/2 : pad + (1-(v-minV)/(maxV-minV)) * (H - 2*pad);
  const colors = ['#4f8cff','#ff8c4f','#4fd08c','#d04f8c','#c08cff'];
  const paths = seriesList.map((s,si) => {
    const pts = s.points || [];
    const d = pts.map((p,k) => `${k?'L':'M'}${sx(p[0]).toFixed(1)},${sy(p[1]).toFixed(1)}`).join(' ');
    return `<path d="${d}" fill="none" stroke="${s.color||colors[si%colors.length]}" stroke-width="1.5"/>`;
  }).join('');
  const zero = (minV < 0 && maxV > 0) ? `<line x1="${pad}" y1="${sy(0).toFixed(1)}" x2="${W-pad}" y2="${sy(0).toFixed(1)}" stroke="#3a4a66" stroke-width="0.5"/>` : '';
  return `<svg viewBox="0 0 ${W} ${H}" width="100%" height="${H}" preserveAspectRatio="none">${zero}${paths}</svg>`;
}

function fmtYoy(y){ return (y==null) ? '—' : (y>0?'+':'') + y + '%'; }

function macroCard(s){
  return `<div class="card" data-ind="${esc(s.indicator)}" style="margin:0">
    <div class="meta-row" style="margin:0">${esc(s.country)}</div>
    <h3 style="margin:2px 0">${esc(s.indicator)}</h3>
    <div><span style="font-size:20px;font-weight:700">${esc(s.latest)}</span> <span class="meta-row" style="display:inline">${esc(s.unit)}</span></div>
    <div class="meta-row">最新 ${esc(s.latest_date)}　前年比 ${esc(fmtYoy(s.yoy))}</div>
    ${lineChartSVG([{points:s.history}])}
  </div>`;
}

function renderMacro(){
  const el = document.getElementById('view-macro');
  const macro = (DATA.macro && DATA.macro.series) || [];
  if(!macro.length){
    el.innerHTML = '<div class="card meta-row">マクロデータがありません。<code>python fetch_macro.py</code> を実行してください。</div>';
    return;
  }
  const inds = [...new Set(macro.map(s => s.indicator))];
  el.innerHTML = `
    <div class="card">
      <select id="macro-ind" style="padding:8px; background:var(--bg); color:var(--txt); border:1px solid var(--line); border-radius:6px;">
        <option value="">すべての指標</option>
        ${inds.map(i => `<option>${esc(i)}</option>`).join('')}
      </select>
    </div>
    <div id="macro-grid" style="display:grid; grid-template-columns:repeat(auto-fill,minmax(280px,1fr)); gap:12px">
      ${macro.map(macroCard).join('')}
    </div>`;
  document.getElementById('macro-ind').addEventListener('change', e => {
    const v = e.target.value;
    document.querySelectorAll('#macro-grid .card').forEach(c =>
      c.style.display = (!v || c.dataset.ind === v) ? '' : 'none');
  });
}
```

- [ ] **Step 5: `init()` に `renderMacro()` を追加**

`init()` 内の `renderAnalysis();` の直後に追記:

```javascript
  renderMacro();
```

- [ ] **Step 6: ブラウザで確認**

`.claude/launch.json` のサーバー `g7-static` を起動し、`index.html` を開く（macro未取得なら案内文を確認）。
**サンプルmacroデータでの確認**: 一時的に `macro_data/M2SL.csv` を作成し `python build.py` 後にリロード。
```
date,value,yoy_pct
2024-01-01,21000,2.5
2025-01-01,21600,2.9
```
Expected: 「マクロ」タブに米国/マネーサプライ(M2)のカード（最新値21600・前年比+2.9%・折れ線）が表示。指標セレクトで絞り込める。確認後、この仮CSVは削除して `python build.py` で戻す。

- [ ] **Step 7: Commit**

```bash
git add index.html
git commit -m "feat: macro view with inline SVG charts"
```

---

## Task 7: index.html — 「マネーの流れ」ビュー

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `DATA.macro.series`, `DATA.macro_analysis`, `lineChartSVG`, `indexTo100`, `fmtYoy`, `esc`
- Produces: `legendHTML(lines)`, `renderMoneyFlow()`。`init` を更新。

- [ ] **Step 1: 「マネーの流れ」ビューを実装**

`renderMacro` の後ろに追記:

```javascript
function legendHTML(lines){
  return '<div class="meta-row">' +
    lines.map(l => `<span style="color:${l.color}">●</span> ${esc(l.label)}`).join('　') +
    '</div>';
}

function renderMoneyFlow(){
  const el = document.getElementById('view-moneyflow');
  const macro = (DATA.macro && DATA.macro.series) || [];
  const a = DATA.macro_analysis || {};
  const colors = ['#4f8cff','#ff8c4f','#4fd08c','#d04f8c','#c08cff'];
  const m2 = macro.filter(s => s.indicator.indexOf('マネーサプライ') === 0);
  const bs = macro.filter(s => s.indicator === '中銀バランスシート');
  const rates = macro.filter(s => s.indicator === '政策金利' || s.indicator === '10年国債利回り');

  const m2lines = m2.map((s,i) => ({label:s.country, points:s.history_yoy||[], color:colors[i%colors.length]}));
  const bslines = bs.map((s,i) => ({label:s.country, points:indexTo100(s.history||[]), color:colors[i%colors.length]}));

  const analysisCard = (a.global_liquidity || a.fx_rates || a.implications) ? `
    <div class="card">
      <h3>考察：世界の流動性</h3>
      ${a.global_liquidity ? `<div>${esc(a.global_liquidity)}</div>` : ''}
      ${a.fx_rates ? `<div style="margin-top:6px"><strong>為替・金利:</strong> ${esc(a.fx_rates)}</div>` : ''}
      ${a.implications ? `<div style="margin-top:6px"><strong>投資の含意:</strong> ${esc(a.implications)}</div>` : ''}
      <div class="meta-row">更新: ${esc(a.updated || '—')}</div>
    </div>` : '';

  if(!macro.length){
    el.innerHTML = '<div class="card meta-row">マクロデータがありません。<code>python fetch_macro.py</code> を実行してください。</div>';
    return;
  }
  el.innerHTML = `
    ${analysisCard}
    <div class="card">
      <h3>マネーサプライ(M2) 前年比 ── 主要国比較</h3>
      ${legendHTML(m2lines)}
      ${lineChartSVG(m2lines, {h:140})}
      <div class="meta-row">最新前年比: ${m2.map(s => `${esc(s.country)} ${esc(fmtYoy(s.yoy))}`).join('　') || '—'}</div>
    </div>
    <div class="card">
      <h3>中銀バランスシート ── 起点=100で比較（拡大/縮小）</h3>
      ${legendHTML(bslines)}
      ${lineChartSVG(bslines, {h:140})}
    </div>
    <div class="card">
      <h3>金利</h3>
      <table style="width:100%; border-collapse:collapse">
        <tr class="meta-row"><td>国</td><td>指標</td><td>最新</td><td>日付</td></tr>
        ${rates.map(s => `<tr><td>${esc(s.country)}</td><td>${esc(s.indicator)}</td><td>${esc(s.latest)}%</td><td class="meta-row">${esc(s.latest_date)}</td></tr>`).join('') || '<tr><td class="meta-row">データなし</td></tr>'}
      </table>
    </div>`;
}
```

- [ ] **Step 2: `init()` に `renderMoneyFlow()` を追加**

`init()` 内の `renderMacro();` の直後に追記:

```javascript
  renderMoneyFlow();
```

- [ ] **Step 3: ブラウザで確認**

仮CSV（Task 6 の M2SL.csv に加え、比較用に2か国分）を用意し `python build.py` 後にリロード:
```
# macro_data/MYAGM2JPM189S.csv
date,value,yoy_pct
2024-01-01,1200,1.8
2025-01-01,1230,2.5
```
Expected: 「マネーの流れ」タブに、M2前年比の複数国折れ線＋凡例、中銀BS（起点100）枠、金利テーブル、（`macro_analysis`があれば）考察カードが表示。確認後、仮CSVを削除して `python build.py` で戻す。

- [ ] **Step 4: 全テスト確認**

Run: `python -m pytest -v`
Expected: 全PASS（`test_build.py` + `test_fetch_macro.py`）。

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat: money-flow view comparing M2/central-bank balance sheets"
```

---

## Task 8: data.json考察フィールド・ドキュメント・実データ取得・最終検証

**Files:**
- Modify: `data.json`, `更新手順.md`
- Create(生成): `macro_data/*.csv`

**Interfaces:**
- Consumes: 全タスクの成果物。
- Produces: `data.json.macro_analysis`、実データ反映の `data.js`・Excel。

- [ ] **Step 1: `data.json` に `macro_analysis` を追加**

`data.json` の `"meta": { ... }` の直後（`"meetings"` の前）に追記:

```json
  "macro_analysis": {
    "updated": "2026-06-22",
    "global_liquidity": "（週次更新で記述）主要中銀のM2前年比と総資産の方向から、世界の流動性が拡大/縮小どちらの局面かを要約する。",
    "fx_rates": "（記述）日米欧の金利差・実質金利からドル円等の地合いを整理する。",
    "implications": "（記述）流動性局面に応じたリスク資産（株・金・暗号資産）への含意。"
  },
```

Run: `python -c "import json; json.load(open('data.json',encoding='utf-8')); print('ok')"`
Expected: `ok`

- [ ] **Step 2: `更新手順.md` にマクロ手順を追記**

`更新手順.md` の末尾に追記:

```markdown

## マクロ数値データ（週1回）
1. 初回のみ: FRED無料APIキーを取得し `fred_api_key.txt` に1行で保存
   （https://fred.stlouisfed.org/docs/api/api_key.html）。このファイルはコミットしない。
2. `python fetch_macro.py` … FREDから `macro_data/*.csv` を更新（未取得シリーズは標準エラーに表示）。
3. `data.json` の `macro_analysis`（global_liquidity / fx_rates / implications / updated）を最新の数値を踏まえて記述。
4. `python build.py` … `data.js` と Excel を再生成。
5. `data.json` と `macro_data/` をGitコミット（履歴保存）。`fred_api_key.txt` はコミットしない。

### 対象シリーズの追加・修正
`series_config.json` に `{id, country, indicator, unit, transform}` を追加。
`transform` は `level`（原系列）または `yoy_pct_also`（前年比%も併記）。
FREDに存在しないidは取得時にスキップ＆ログ表示されるので、ログを見て修正する。

## 主要国財務省の政策（質的情報）
G7声明と同様、`data.json` の `meetings[]` に **種別「財務省政策」** で追記する
（例: 日本MOFの為替介入、米財務省の国債発行計画、中国の財政刺激）。既存の会議ログ・
考察ビュー・Excelにそのまま反映される（コード変更不要）。
```

- [ ] **Step 3: FRED APIキーを配置（ユーザー作業）**

ユーザーが取得した無料キーを `fred_api_key.txt` に保存。
確認: `python -c "import fetch_macro as m; print(len(m.load_api_key())>0)"`
Expected: `True`（キー未配置なら、ここで停止しユーザーに取得を依頼）

- [ ] **Step 4: 実データを取得**

Run: `python fetch_macro.py`
Expected: `OK: N series 取得`。未取得シリーズがあれば標準エラーに一覧表示（その分は `series_config.json` のidを後で見直す）。`macro_data/*.csv` が生成される。

- [ ] **Step 5: ビルドして生成物を確認**

Run: `python build.py`
Expected: `OK: 7 meetings, 19 calendar events`。`data.js` にmacroが入り、Excelに `マクロ_最新`/`マクロ_時系列` が反映。

- [ ] **Step 6: テストとブラウザ最終確認**

Run: `python -m pytest -v`
Expected: 全PASS。
ブラウザで「マクロ」「マネーの流れ」に実データのカード・チャートが表示され、コンソールエラーが無いことを確認。Excelの2シートに最新値・時系列が入っていることを確認。

- [ ] **Step 7: Commit**

```bash
git add data.json 更新手順.md series_config.json macro_data
git commit -m "data: macro_analysis field, docs, and initial FRED data"
```

---

## 完了の定義（受け入れ基準）

- [ ] `python fetch_macro.py` でG7＋中国の主要指標が `macro_data/*.csv` に保存される（未取得は明示）。
- [ ] `fred_api_key.txt` はGit追跡されない（`.gitignore` 済み）。
- [ ] ダッシュボードに「マクロ」「マネーの流れ」が追加され、最新値・前年比・SVG折れ線が表示される。
- [ ] 「マネーの流れ」で主要国のM2前年比・中銀BS（起点100）が比較でき、`macro_analysis` の考察が表示される。
- [ ] Excelに `マクロ_最新`・`マクロ_時系列` が追加され、既存3シートも維持される。
- [ ] `python -m pytest` が全PASS（既存テストを壊さない）。
- [ ] 画面・Excelに免責が明記されている。
