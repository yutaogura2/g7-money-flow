# 運用・配信（サブC）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox (`- [ ]`).

**Goal:** 週次ブリーフィングの自動生成・データ鮮度フラグ・チャートのホバー値表示を追加し、ダッシュボードを「読みやすく自動配信」される形にする。

**Architecture:** build.py が signals/stale から定型ブリーフィングを生成し payload＋`週次ブリーフィング.md` に出力。index.html は概要にブリーフィング、カードに鮮度バッジ、チャートにホバーを追加。

**Tech Stack:** Python 3.12（標準ライブラリ）、Vanilla JS、pytest。新規依存なし。

## Global Constraints
- 配置ルート相対パス。UTF-8。ブリーフィングはデータ由来の定型生成のみ（推測なし）。
- 鮮度は観測間隔の中央値ベース（`stale_days > median_gap*2.5 + 7`）。観測2点未満は (0,False)。
- ホバーは単一系列スパークラインのみ（点は60に間引き）。複数系列チャートは対象外。
- 既存テストを壊さない。Gitはmain、各タスク末尾でコミット。
- 既存: `build._read_series_csv/_series_stats/_value_on_or_before/build_signals/build_macro_payload/main`、`index.html` の `renderOverview/macroCard/lineChartSVG/signalBadge/esc/init`。

---

## File Structure
| パス | 区分 | 責務 |
|---|---|---|
| `build.py` | 変更 | `_staleness`、payloadに stale、`build_briefing`/`write_briefing_md`、payloadに briefing、main で書出 |
| `tests/test_build.py` | 変更 | 鮮度・ブリーフィングのテスト |
| `index.html` | 変更 | 概要ブリーフィング、鮮度バッジ、チャートホバー |
| `更新手順.md` | 変更 | 週次ブリーフィングの説明 |

---

## Task 1: データ鮮度（_staleness）と payload付与

**Files:** Modify `build.py`, `tests/test_build.py`

**Interfaces:**
- `_staleness(rows: list, today: date) -> tuple[int, bool]`（rows=`[(date,val,yoy)]`、観測間隔中央値で判定）
- payload各系列に `stale_days`・`stale`。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` 末尾に追記:

```python
from datetime import date as _date


def test_staleness_monthly_and_quarterly():
    monthly = [(f"2026-{m:02d}-01", 1.0, None) for m in range(1, 6)]  # Jan..May
    assert build._staleness(monthly, _date(2026, 6, 5))[1] is False   # ~35日
    assert build._staleness(monthly, _date(2026, 10, 1))[1] is True   # ~153日
    quarterly = [("2025-09-01", 1.0, None), ("2025-12-01", 1.0, None), ("2026-03-01", 1.0, None)]
    assert build._staleness(quarterly, _date(2026, 6, 5))[1] is False  # ~96日 < 91*2.5+7
    assert build._staleness([("2026-01-01", 1.0, None)], _date(2026, 6, 5)) == (0, False)
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_build.py -k staleness -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`build.py` の `_value_on_or_before` の後ろに追記:

```python
def _staleness(rows: list, today: date) -> tuple:
    dates = [r[0] for r in rows]
    if len(dates) < 2:
        return (0, False)
    ds = [date.fromisoformat(x) for x in dates[-13:]]
    gaps = [(ds[i] - ds[i - 1]).days for i in range(1, len(ds))]
    gaps = sorted(g for g in gaps if g > 0)
    med = gaps[len(gaps) // 2] if gaps else 30
    stale_days = (today - date.fromisoformat(dates[-1])).days
    return (stale_days, stale_days > med * 2.5 + 7)
```

`build_macro_payload` 内、`stats = _series_stats(values)` の後ろに追記:

```python
        stale_days, stale = _staleness(rows, date.today())
```

`series.append({...})` の辞書、`"delta": delta, "delta_z": delta_z,` の直後に追記:

```python
            "stale_days": stale_days, "stale": stale,
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_build.py -k staleness -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add build.py tests/test_build.py
git commit -m "feat: data staleness detection per series"
```

---

## Task 2: 週次ブリーフィング（build_briefing / write_briefing_md）

**Files:** Modify `build.py`, `tests/test_build.py`

**Interfaces:**
- `build_briefing(series: list, signals: dict, as_of: str) -> dict`（`{as_of, lines:[...]}`）
- `write_briefing_md(briefing: dict, path=ROOT/"週次ブリーフィング.md") -> None`
- `build_macro_payload` の戻り値に `briefing` を追加。`main()` で md 書出。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` 末尾に追記:

```python
def test_build_briefing_lines():
    series = [
        {"indicator": "ボラティリティ", "country": "VIX", "zscore": 2.5, "stale": False, "stale_days": 3},
        {"indicator": "建玉(COT)", "country": "円", "zscore": -2.18, "stale": False, "stale_days": 9},
        {"indicator": "CPI", "country": "米国", "zscore": 0.3, "stale": True, "stale_days": 400},
    ]
    signals = {"regime": {"label": "リスクオフ", "score": 0.6, "level": "warn"},
               "movers": [{"indicator": "商品", "country": "WTI原油", "delta_z": -1.2, "dir": "down"}]}
    b = build.build_briefing(series, signals, "2026-06-25")
    text = " ".join(b["lines"])
    assert b["as_of"] == "2026-06-25"
    assert "リスクオフ" in text
    assert "極端な水準" in text and "VIX" in text
    assert "ポジション偏り" in text and "円" in text
    assert "⚠" in text and "CPI" in text


def test_write_briefing_md(tmp_path):
    p = tmp_path / "b.md"
    build.write_briefing_md({"as_of": "2026-06-25", "lines": ["a", "b"]}, path=p)
    t = p.read_text(encoding="utf-8")
    assert "週次ブリーフィング 2026-06-25" in t and "- a" in t


def test_payload_includes_briefing(tmp_path):
    _write_macro_fixture(tmp_path)
    p = build.build_macro_payload(
        config_path=tmp_path / "series_config.json", data_dir=tmp_path / "macro_data")
    assert "briefing" in p and "lines" in p["briefing"]
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_build.py -k "briefing" -v`
Expected: FAIL（未定義 / briefing無し）

- [ ] **Step 3: 実装を追加**

`build.py` の `build_signals` の後ろに追記:

```python
def build_briefing(series: list, signals: dict, as_of: str) -> dict:
    lines = []
    reg = signals.get("regime", {})
    lines.append(f"リスクレジーム: {reg.get('label', '—')} (総合スコア {reg.get('score')})")
    movers = signals.get("movers", [])[:5]
    if movers:
        parts = [f"{'▲' if m['dir'] == 'up' else '▼'}{m['indicator']}・{m['country']}(Δz {m['delta_z']})" for m in movers]
        lines.append("直近の主な変化: " + " / ".join(parts))
    ext = sorted([s for s in series if s.get("zscore") is not None and abs(s["zscore"]) >= 2],
                 key=lambda s: abs(s["zscore"]), reverse=True)
    if ext:
        parts = [f"{s['indicator']}・{s['country']} z{s['zscore']}({'高' if s['zscore'] > 0 else '低'})" for s in ext]
        lines.append("極端な水準(|Z|≥2): " + " / ".join(parts))
    pos = sorted([s for s in series if s.get("indicator") == "建玉(COT)" and s.get("zscore") is not None and abs(s["zscore"]) >= 1.5],
                 key=lambda s: abs(s["zscore"]), reverse=True)
    if pos:
        parts = [f"{s['country']} z{s['zscore']}({'買い過熱' if s['zscore'] > 0 else '売り過熱'})" for s in pos]
        lines.append("ポジション偏り(建玉|Z|≥1.5): " + " / ".join(parts))
    stale = [s for s in series if s.get("stale")]
    if stale:
        lines.append("データ鮮度: ⚠ " + " / ".join(f"{s['indicator']}・{s['country']}({s['stale_days']}日)" for s in stale))
    else:
        lines.append("データ鮮度: 異常なし")
    return {"as_of": as_of, "lines": lines}


def write_briefing_md(briefing: dict, path: Path = ROOT / "週次ブリーフィング.md") -> None:
    md = f"# 週次ブリーフィング {briefing['as_of']}\n\n" + "\n".join(f"- {ln}" for ln in briefing["lines"]) + "\n"
    Path(path).write_text(md, encoding="utf-8")
```

`build_macro_payload` の `return {"series": series, "signals": build_signals(series)}` を次に置換:

```python
    signals = build_signals(series)
    briefing = build_briefing(series, signals, date.today().isoformat())
    return {"series": series, "signals": signals, "briefing": briefing}
```

`main()` の `data["macro"] = build_macro_payload()` の直後に追記:

```python
    write_briefing_md(data["macro"]["briefing"])
```

- [ ] **Step 4: テストとビルド確認**

Run: `python -m pytest tests/test_build.py -k briefing -v`
Expected: PASS。
Run: `python build.py`
Expected: `OK: ...`。`週次ブリーフィング.md` が生成される。

- [ ] **Step 5: Commit**

```bash
git add build.py tests/test_build.py "週次ブリーフィング.md"
git commit -m "feat: weekly auto briefing (template) + markdown output"
```

---

## Task 3: 概要ブリーフィング表示と鮮度バッジ（index.html）

**Files:** Modify `index.html`

**Interfaces:** `renderOverview` でサブAのレジーム/ムーバーカードをブリーフィングカードに置換。`macroCard` に鮮度バッジ。

- [ ] **Step 1: renderOverview のレジーム/ムーバーをブリーフィングに置換**

`index.html` の `renderOverview` 内、サブAで追加した変数群（`const sig = ...` から `const movers = ...` まで）を次に置換:

```javascript
  const brief = (DATA.macro && DATA.macro.briefing) || { lines: [] };
```

同関数内、`el.innerHTML = \`` 直後に挿入していた「リスクレジーム」カードと「直近の主な変化」カードの2ブロックを、次の1ブロックに置換:

```javascript
    <div class="card">
      <h3>週次ブリーフィング <span class="meta-row" style="display:inline">${esc(brief.as_of || '')}</span></h3>
      ${(brief.lines || []).map(l => `<div class="meta-row" style="font-size:13px; margin-bottom:4px">${esc(l)}</div>`).join('') || '<div class="meta-row">—</div>'}
    </div>
```

- [ ] **Step 2: macroCard に鮮度バッジを追加**

`macroCard` 内、`${signalBadge(s)}` の直後に追記:

```javascript
    ${s.stale ? `<div class="meta-row" style="color:#ffb74f">⚠ データ古い (${esc(s.stale_days)}日)</div>` : ''}
```

- [ ] **Step 3: ビルドしてブラウザ確認**

Run: `python build.py`
`g7-static` 起動、`index.html` 再読込。
Expected: 概要先頭に「週次ブリーフィング」カード（リスクレジーム・変化・極端・ポジション・鮮度の行）。サブAのレジームパネル/ムーバーカードは消えている。古い系列のカードに ⚠ バッジ。コンソールエラーなし。

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat: overview briefing card + staleness badge"
```

---

## Task 4: チャート ホバー（C10）

**Files:** Modify `index.html`

**Interfaces:** `lineChartSVG` 単一系列を `.spark`（`data-pts`）で包む。`attachSparkHover()` を `init` で1度呼ぶ。

- [ ] **Step 1: lineChartSVG を spark ラッパに**

`index.html` の `lineChartSVG` の `return ...` 行を次に置換:

```javascript
  const svg = `<svg viewBox="0 0 ${W} ${H}" width="100%" height="${H}" preserveAspectRatio="none">${zero}${paths}</svg>`;
  if(seriesList.length === 1){
    const pts = seriesList[0].points || [];
    const step = pts.length > 60 ? Math.ceil(pts.length / 60) : 1;
    const dpts = pts.filter((_, i) => i % step === 0);
    return `<div class="spark" style="position:relative" data-pts='${JSON.stringify(dpts)}'>${svg}<div class="spark-tip" style="position:absolute; top:0; display:none; background:#0b1424; border:1px solid var(--line); color:var(--txt); font-size:11px; padding:1px 5px; border-radius:4px; pointer-events:none; white-space:nowrap"></div></div>`;
  }
  return svg;
```

（`return \`<svg ...>${zero}${paths}</svg>\`;` を上記で置換。`zero`/`paths`/`W`/`H` は既存のまま。）

- [ ] **Step 2: attachSparkHover を追加し init で呼ぶ**

`signalBadge` 関数の直後に追記:

```javascript
function attachSparkHover(){
  document.addEventListener('mousemove', e => {
    const spark = e.target.closest ? e.target.closest('.spark') : null;
    document.querySelectorAll('.spark-tip').forEach(t => {
      if(!spark || !spark.contains(t)) t.style.display = 'none';
    });
    if(!spark) return;
    let pts;
    try { pts = JSON.parse(spark.getAttribute('data-pts') || '[]'); } catch(_){ return; }
    if(!pts.length) return;
    const rect = spark.getBoundingClientRect();
    const frac = Math.min(1, Math.max(0, (e.clientX - rect.left) / rect.width));
    const p = pts[Math.round(frac * (pts.length - 1))];
    const tip = spark.querySelector('.spark-tip');
    if(tip && p){
      tip.textContent = `${p[0]}: ${p[1]}`;
      tip.style.display = 'block';
      tip.style.left = Math.min(rect.width - 96, Math.max(0, e.clientX - rect.left)) + 'px';
    }
  });
}
```

`init()` の末尾（`renderMarket();` の直後）に追記:

```javascript
  attachSparkHover();
```

- [ ] **Step 3: ブラウザ確認**

`index.html` 再読込。カードのチャートにマウスを乗せると「日付: 値」のツールチップが追従表示される。離すと消える。コンソールエラーなし。

- [ ] **Step 4: 全テスト確認**

Run: `python -m pytest -q`
Expected: 全PASS。

- [ ] **Step 5: 更新手順.md にブリーフィングを追記しCommit**

`更新手順.md` の「## 自動更新（Windowsタスクスケジューラ）」セクション末尾に追記:

```markdown
- 週次ブリーフィング: build時に `週次ブリーフィング.md` を自動生成（リスクレジーム・主な変化・極端な水準・ポジション偏り・データ鮮度）。概要タブにも表示。
```

```bash
git add index.html 更新手順.md
git commit -m "feat: chart hover tooltips + briefing docs"
```

---

## 完了の定義
- [ ] 概要に「週次ブリーフィング」（レジーム/変化/極端/ポジション/鮮度）が表示される。
- [ ] `週次ブリーフィング.md` が生成・更新される。
- [ ] 古い系列に ⚠ バッジ。チャートにホバーで日付＋値。
- [ ] `python -m pytest` 全PASS（既存を壊さない）。
