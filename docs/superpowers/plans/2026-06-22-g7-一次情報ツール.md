# G7一次情報まとめ・考察ツール 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** G7財務相・中銀総裁会議／首脳会議の公式一次情報を日本語に翻訳・要約し、投資観点の考察を付けて、ローカルのブラウザ・ダッシュボードとExcel全archiveで閲覧できるツールを作る。

**Architecture:** single source of truth は `data.json`。`build.py` がそこから `data.js`（ブラウザ用）と `G7_一次情報ログ.xlsx`（Excel）を生成する。`index.html` は `data.js` を `<script src>` で読むだけなので `file://` で動作（サーバー不要・オフライン）。私（Claude）が公式ソースを巡回して `data.json` に追記し、`build.py` を実行する運用。

**Tech Stack:** Python 3.12（openpyxl）、Vanilla HTML/CSS/JavaScript（フレームワーク無し）、pytest（build.pyのテスト）。

## Global Constraints

- 配置ルート: `C:\Users\yutao\OneDrive\デスクトップ\★Claud code★\銀行一次情報`（以降パスはここからの相対）。
- 文字コードはすべて **UTF-8**。Python のファイル入出力は必ず `encoding="utf-8"`。
- single source of truth は `data.json` のみ。`data.js` と `G7_一次情報ログ.xlsx` は必ず `build.py` で生成し、手で編集しない。
- `index.html` は外部fetchを使わず、`data.js`（`window.G7DATA = {...};`）を `<script src>` で読む（`file://` のCORS制約回避）。
- 翻訳は原文の意味を保持。出典が確認できない情報は記載しない（推測で埋めない）。
- 画面・Excel・README に免責（考察は参考情報であり投資助言ではない）を明記。
- Python 3.12.10 / pip 25.0.1 が利用可能。openpyxl は未インストール（Task 1 で導入）。
- 依存は最小（openpyxl、pytest のみ）。
- Git は任意。各タスク末尾の「Checkpoint」はGitを使う場合のみコミット（未初期化なら作業ツリー保存のみでよい）。

---

## File Structure

| パス | 責務 |
|---|---|
| `data.json` | データ本体（meta / meetings / calendar）。手で追記する唯一の源 |
| `build.py` | data.json → data.js + Excel を生成 |
| `data.js` | 生成物。`window.G7DATA = {...};` |
| `G7_一次情報ログ.xlsx` | 生成物。3シートのExcel archive |
| `index.html` | ダッシュボード（CSS/JS内蔵、4ビュー） |
| `requirements.txt` | 依存（openpyxl, pytest） |
| `tests/test_build.py` | build.py のpytestテスト |
| `README.md` | 使い方 |
| `更新手順.md` | 更新手順・対象公式ソース一覧 |

---

## Task 1: プロジェクト雛形と依存セットアップ

**Files:**
- Create: `requirements.txt`
- Create: `data.json`（開発用の最小サンプル）
- Create: `tests/` ディレクトリ

**Interfaces:**
- Produces: `data.json` のスキーマ（`meta`, `meetings[]`, `calendar[]`）。後続タスクが消費。

- [ ] **Step 1: requirements.txt を作成**

```
openpyxl==3.1.5
pytest==8.3.4
```

- [ ] **Step 2: 依存をインストール**

Run: `python -m pip install -r requirements.txt`
Expected: openpyxl と pytest が正常にインストールされる（`Successfully installed ...`）。

- [ ] **Step 3: 開発用サンプル data.json を作成**

`data.json`（後でTask 9が実データに置き換える。今はビルド/表示確認用の最小2件）:

```json
{
  "meta": {
    "last_updated": "2026-06-22",
    "sources": [
      { "name": "米国財務省", "url": "https://home.treasury.gov/", "lang": "en" },
      { "name": "日本 財務省", "url": "https://www.mof.go.jp/", "lang": "ja" }
    ],
    "disclaimer": "本ツールの考察は一次情報をもとにした参考情報であり、投資助言・売買推奨ではありません。最終的な投資判断はご自身の責任で行ってください。"
  },
  "meetings": [
    {
      "id": "sample-2025-fmcbg",
      "date": "2025-05-22",
      "type": "財務相・中銀総裁会議",
      "presidency": "カナダ",
      "title_orig": "G7 Finance Ministers and Central Bank Governors' Statement (SAMPLE)",
      "title_ja": "G7財務相・中央銀行総裁声明（サンプル）",
      "source_url": "https://example.com/sample-a",
      "source_lang": "en",
      "summary_ja": "（サンプル要約）為替の過度な変動への警戒を再確認し、対ロシア制裁の継続で一致。",
      "key_points_ja": ["為替の過度な変動・無秩序な動きは経済に悪影響との文言を維持", "対ロシア制裁の継続と凍結資産の活用を協議"],
      "full_translation_ja": "（サンプル全訳）我々は、為替レートの過度な変動および無秩序な動きが経済および金融の安定に悪影響を及ぼし得ることを再確認する……",
      "analysis": {
        "fx_rates": "為替の文言維持は急激な円安局面での協調介入余地を残す。ドル円の上値抑制要因。",
        "commodities": "エネルギー制裁の継続は原油・ガスの供給プレミアム要因。",
        "sectors": "制裁継続は防衛・資源にプラス、対ロシア依存業種にマイナス。",
        "geopolitics": "対ロシア結束維持。リスクオフ材料の燻り。"
      },
      "market_direction": "ドル円: 上値重い／リスク: ややオフ",
      "tags": ["為替", "対ロシア制裁", "凍結資産"]
    },
    {
      "id": "sample-2025-summit",
      "date": "2025-06-17",
      "type": "首脳会議",
      "presidency": "カナダ",
      "title_orig": "G7 Leaders' Statement (SAMPLE)",
      "title_ja": "G7首脳声明（サンプル）",
      "source_url": "https://example.com/sample-b",
      "source_lang": "en",
      "summary_ja": "（サンプル要約）経済安全保障と重要鉱物のサプライチェーン強靱化で合意。",
      "key_points_ja": ["重要鉱物のサプライチェーン強靱化", "AI・半導体の輸出管理協調"],
      "full_translation_ja": "（サンプル全訳）我々G7首脳は、経済安全保障を強化し……",
      "analysis": {
        "fx_rates": "直接の為替言及は限定的。",
        "commodities": "重要鉱物の囲い込みは関連資源価格の支援要因。",
        "sectors": "半導体・防衛・重要鉱物関連にプラス。",
        "geopolitics": "対中デリスキングの方向性を再確認。"
      },
      "market_direction": "重要鉱物・半導体関連にフォロー",
      "tags": ["経済安全保障", "重要鉱物", "半導体"]
    }
  ],
  "calendar": [
    { "date": "2025-05-22", "name_ja": "G7財務相・中銀総裁会議（カナダ）", "category": "G7財務", "status": "済", "related_meeting_id": "sample-2025-fmcbg", "note": "議長国カナダ" },
    { "date": "2025-06-17", "name_ja": "G7首脳会議（カナダ）", "category": "G7首脳", "status": "済", "related_meeting_id": "sample-2025-summit", "note": "議長国カナダ" },
    { "date": "2026-06-15", "name_ja": "G7首脳会議（議長国フランス）", "category": "G7首脳", "status": "予定", "related_meeting_id": null, "note": "2026年議長国フランス（会場・日程は要確認）" }
  ]
}
```

- [ ] **Step 4: tests ディレクトリを用意**

Run: `python -c "import os; os.makedirs('tests', exist_ok=True); open('tests/__init__.py','a').close()"`
Expected: `tests/` と空の `tests/__init__.py` が作成される。

- [ ] **Step 5: data.json が妥当なJSONであることを確認**

Run: `python -c "import json; d=json.load(open('data.json',encoding='utf-8')); print(len(d['meetings']),'meetings', len(d['calendar']),'events')"`
Expected: `2 meetings 3 events`

- [ ] **Step 6: Checkpoint（Git使用時のみコミット）**

```bash
git add requirements.txt data.json tests/__init__.py
git commit -m "chore: project scaffolding and sample data"
```

---

## Task 2: build.py — data.js 生成

**Files:**
- Create: `build.py`
- Test: `tests/test_build.py`

**Interfaces:**
- Produces:
  - `build.load_data(path=DATA_JSON) -> dict`
  - `build.write_data_js(data: dict, path=DATA_JS) -> None`（`data.js` に `window.G7DATA = <json>;\n` を書く）
  - 定数 `build.DATA_JSON`, `build.DATA_JS`（`Path`）

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_build.py`:

```python
import json
import re
from pathlib import Path

import pytest

import build

SAMPLE = {
    "meta": {"last_updated": "2026-06-22", "sources": [], "disclaimer": "参考情報"},
    "meetings": [
        {
            "id": "m1", "date": "2025-05-22", "type": "財務相・中銀総裁会議",
            "presidency": "カナダ", "title_orig": "G7 FMCBG Statement",
            "title_ja": "G7財務相・中銀総裁声明", "source_url": "https://example.com/a",
            "source_lang": "en", "summary_ja": "要約A",
            "key_points_ja": ["決定1", "決定2"], "full_translation_ja": "全訳A",
            "analysis": {"fx_rates": "為替A", "commodities": "商品A",
                         "sectors": "セクターA", "geopolitics": "地政学A"},
            "market_direction": "リスクオフ", "tags": ["制裁", "為替"],
        }
    ],
    "calendar": [
        {"date": "2026-06-15", "name_ja": "G7首脳会議", "category": "G7首脳",
         "status": "予定", "related_meeting_id": None, "note": "議長国フランス"}
    ],
}


@pytest.fixture
def sample_json(tmp_path):
    p = tmp_path / "data.json"
    p.write_text(json.dumps(SAMPLE, ensure_ascii=False), encoding="utf-8")
    return p


def test_write_data_js_has_prefix_and_valid_json(tmp_path, sample_json):
    data = build.load_data(sample_json)
    out = tmp_path / "data.js"
    build.write_data_js(data, out)
    text = out.read_text(encoding="utf-8")
    assert text.startswith("window.G7DATA = ")
    body = re.sub(r"^window\.G7DATA = ", "", text).rstrip().rstrip(";")
    parsed = json.loads(body)
    assert parsed["meetings"][0]["title_ja"] == "G7財務相・中銀総裁声明"
    # 日本語がエスケープされず読めること
    assert "\\u" not in text
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'build'` または属性未定義）

- [ ] **Step 3: build.py を最小実装**

`build.py`:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""data.json から data.js（ブラウザ用）と Excel を生成する。"""
import json
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent
DATA_JSON = ROOT / "data.json"
DATA_JS = ROOT / "data.js"
XLSX = ROOT / "G7_一次情報ログ.xlsx"


def load_data(path: Path = DATA_JSON) -> dict:
    with open(path, encoding="utf-8") as f:
        return json.load(f)


def write_data_js(data: dict, path: Path = DATA_JS) -> None:
    payload = json.dumps(data, ensure_ascii=False, indent=2)
    Path(path).write_text(f"window.G7DATA = {payload};\n", encoding="utf-8")


def main() -> int:
    data = load_data()
    write_data_js(data)
    print(f"OK: {len(data.get('meetings', []))} meetings, "
          f"{len(data.get('calendar', []))} calendar events")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: PASS

- [ ] **Step 5: 実データで生成を確認**

Run: `python build.py`
Expected: `OK: 2 meetings, 3 calendar events` と表示、`data.js` が生成される。

- [ ] **Step 6: Checkpoint（Git使用時のみ）**

```bash
git add build.py tests/test_build.py
git commit -m "feat: build.py generates data.js from data.json"
```

---

## Task 3: build.py — Excel（G7_一次情報ログ.xlsx）生成

**Files:**
- Modify: `build.py`
- Modify: `tests/test_build.py`

**Interfaces:**
- Consumes: `build.load_data`
- Produces:
  - `build.build_xlsx(data: dict, path=XLSX) -> None`
  - 定数 `build.MEETING_HEADERS`, `build.CALENDAR_HEADERS`, `build.ANALYSIS_HEADERS`（list[str]）
  - シート名は固定で `["会議ログ", "カレンダー", "考察サマリ"]`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` の末尾に追記:

```python
from openpyxl import load_workbook


def test_build_xlsx_sheets_and_rows(tmp_path, sample_json):
    data = build.load_data(sample_json)
    out = tmp_path / "out.xlsx"
    build.build_xlsx(data, out)
    wb = load_workbook(out)
    assert wb.sheetnames == ["会議ログ", "カレンダー", "考察サマリ"]
    assert wb["会議ログ"].max_row == 2       # ヘッダ + 1会議
    assert wb["カレンダー"].max_row == 2      # ヘッダ + 1イベント
    assert wb["考察サマリ"].max_row == 5      # ヘッダ + 4観点
    # ヘッダ確認
    assert wb["会議ログ"].cell(row=1, column=1).value == "日付"


def test_build_xlsx_source_hyperlink(tmp_path, sample_json):
    data = build.load_data(sample_json)
    out = tmp_path / "out.xlsx"
    build.build_xlsx(data, out)
    wb = load_workbook(out)
    ws = wb["会議ログ"]
    url_cell = ws.cell(row=2, column=len(build.MEETING_HEADERS))
    assert url_cell.hyperlink is not None
    assert url_cell.hyperlink.target == "https://example.com/a"
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `python -m pytest tests/test_build.py -k xlsx -v`
Expected: FAIL（`build.build_xlsx` 未定義）

- [ ] **Step 3: build.py に Excel 生成を実装**

`build.py` の import 群に追記:

```python
from openpyxl import Workbook
from openpyxl.styles import Font, Alignment, PatternFill
from openpyxl.utils import get_column_letter
```

`write_data_js` の後ろに追記:

```python
MEETING_HEADERS = [
    "日付", "種別", "議長国", "邦題", "原題", "要約", "決定事項",
    "主要部分の全訳", "考察(為替・金利)", "考察(商品・エネルギー・金)",
    "考察(セクター)", "考察(地政学)", "方向性メモ", "タグ", "原文URL",
]
CALENDAR_HEADERS = ["日付", "名称", "カテゴリ", "状態", "関連会議ID", "備考"]
ANALYSIS_HEADERS = ["日付", "観点", "論点", "関連会議ID"]

ASPECTS = [
    ("fx_rates", "為替・金利"),
    ("commodities", "商品・エネルギー・金"),
    ("sectors", "セクター"),
    ("geopolitics", "地政学"),
]


def _style_header(ws) -> None:
    fill = PatternFill("solid", fgColor="1F3864")
    font = Font(bold=True, color="FFFFFF")
    for cell in ws[1]:
        cell.fill = fill
        cell.font = font
        cell.alignment = Alignment(vertical="center", wrap_text=True)
    ws.freeze_panes = "A2"
    ws.auto_filter.ref = ws.dimensions


def build_xlsx(data: dict, path: Path = XLSX) -> None:
    wb = Workbook()

    ws1 = wb.active
    ws1.title = "会議ログ"
    ws1.append(MEETING_HEADERS)
    for m in data.get("meetings", []):
        a = m.get("analysis", {})
        ws1.append([
            m.get("date", ""), m.get("type", ""), m.get("presidency", ""),
            m.get("title_ja", ""), m.get("title_orig", ""), m.get("summary_ja", ""),
            "\n".join(m.get("key_points_ja", [])), m.get("full_translation_ja", ""),
            a.get("fx_rates", ""), a.get("commodities", ""),
            a.get("sectors", ""), a.get("geopolitics", ""),
            m.get("market_direction", ""), ", ".join(m.get("tags", [])),
            m.get("source_url", ""),
        ])
        url = m.get("source_url", "")
        if url:
            c = ws1.cell(row=ws1.max_row, column=len(MEETING_HEADERS))
            c.hyperlink = url
            c.font = Font(color="0563C1", underline="single")
    _style_header(ws1)

    ws2 = wb.create_sheet("カレンダー")
    ws2.append(CALENDAR_HEADERS)
    for e in sorted(data.get("calendar", []), key=lambda x: x.get("date", "")):
        ws2.append([
            e.get("date", ""), e.get("name_ja", ""), e.get("category", ""),
            e.get("status", ""), e.get("related_meeting_id") or "", e.get("note", ""),
        ])
    _style_header(ws2)

    ws3 = wb.create_sheet("考察サマリ")
    ws3.append(ANALYSIS_HEADERS)
    for m in data.get("meetings", []):
        a = m.get("analysis", {})
        for key, label in ASPECTS:
            text = a.get(key, "")
            if text:
                ws3.append([m.get("date", ""), label, text, m.get("id", "")])
    _style_header(ws3)

    for ws in wb.worksheets:
        for col in range(1, ws.max_column + 1):
            ws.column_dimensions[get_column_letter(col)].width = 24

    Path(path).parent.mkdir(parents=True, exist_ok=True)
    wb.save(path)
```

`main()` を更新（`write_data_js(data)` の次の行に追記）:

```python
    build_xlsx(data)
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: 実データで生成を確認**

Run: `python build.py`
Expected: `OK: 2 meetings, 3 calendar events`、`G7_一次情報ログ.xlsx` が生成される。

- [ ] **Step 6: Checkpoint（Git使用時のみ）**

```bash
git add build.py tests/test_build.py
git commit -m "feat: build.py generates Excel archive with 3 sheets"
```

---

## Task 4: index.html — 雛形・タブ・概要ビュー

**Files:**
- Create: `index.html`

**Interfaces:**
- Consumes: `window.G7DATA`（`data.js` から）。形は `{meta, meetings[], calendar[]}`。
- Produces: グローバル関数 `init()` と `renderOverview()`、`switchView(name)`。後続タスクが `renderMeetings()`, `renderCalendar()`, `renderAnalysis()` を追加し `init()` に呼び出しを足す。
- セクションの id: `view-overview`, `view-meetings`, `view-calendar`, `view-analysis`。

- [ ] **Step 1: index.html を作成（雛形＋概要）**

`index.html`:

```html
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>G7一次情報ダッシュボード</title>
<style>
  :root { --bg:#0f1729; --panel:#16223b; --line:#27406b; --txt:#e6ecf5; --muted:#9fb2cf; --accent:#4f8cff; --chip:#1f3864; }
  * { box-sizing: border-box; }
  body { margin:0; font-family: "Segoe UI","Yu Gothic UI",Meiryo,sans-serif; background:var(--bg); color:var(--txt); }
  header { padding:16px 20px; border-bottom:1px solid var(--line); display:flex; align-items:baseline; gap:16px; flex-wrap:wrap; }
  header h1 { font-size:20px; margin:0; }
  #last-updated { color:var(--muted); font-size:13px; }
  nav#tabs { display:flex; gap:4px; padding:8px 16px; border-bottom:1px solid var(--line); flex-wrap:wrap; }
  nav#tabs button { background:transparent; color:var(--muted); border:1px solid transparent; padding:8px 14px; border-radius:8px 8px 0 0; cursor:pointer; font-size:14px; }
  nav#tabs button.active { color:var(--txt); background:var(--panel); border-color:var(--line); }
  main { padding:18px 20px; max-width:1100px; }
  .view.hidden { display:none; }
  .card { background:var(--panel); border:1px solid var(--line); border-radius:10px; padding:14px 16px; margin-bottom:12px; }
  .card h3 { margin:0 0 6px; font-size:16px; }
  .meta-row { color:var(--muted); font-size:12px; margin-bottom:8px; }
  .chip { display:inline-block; background:var(--chip); color:#cfe0ff; border-radius:999px; padding:2px 10px; font-size:12px; margin:2px 4px 2px 0; }
  .kpi { display:flex; gap:14px; flex-wrap:wrap; margin-bottom:16px; }
  .kpi .box { background:var(--panel); border:1px solid var(--line); border-radius:10px; padding:12px 16px; min-width:130px; }
  .kpi .num { font-size:24px; font-weight:700; }
  .kpi .lbl { color:var(--muted); font-size:12px; }
  footer#disclaimer { margin-top:24px; padding:14px 20px; border-top:1px solid var(--line); color:var(--muted); font-size:12px; }
  a { color:var(--accent); }
</style>
</head>
<body>
<header>
  <h1>G7一次情報ダッシュボード</h1>
  <div id="last-updated"></div>
</header>
<nav id="tabs">
  <button data-view="overview" class="active">概要</button>
  <button data-view="meetings">会議・声明ログ</button>
  <button data-view="calendar">イベントカレンダー</button>
  <button data-view="analysis">考察ビュー</button>
</nav>
<main>
  <section id="view-overview" class="view"></section>
  <section id="view-meetings" class="view hidden"></section>
  <section id="view-calendar" class="view hidden"></section>
  <section id="view-analysis" class="view hidden"></section>
</main>
<footer id="disclaimer"></footer>

<script src="data.js"></script>
<script>
const DATA = window.G7DATA || { meta:{}, meetings:[], calendar:[] };

function esc(s){ return String(s==null?"":s).replace(/[&<>"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c])); }
function byDateDesc(a,b){ return (b.date||"").localeCompare(a.date||""); }
function todayStr(){ return new Date().toISOString().slice(0,10); }

function switchView(name){
  document.querySelectorAll('nav#tabs button').forEach(b =>
    b.classList.toggle('active', b.dataset.view === name));
  ['overview','meetings','calendar','analysis'].forEach(v =>
    document.getElementById('view-'+v).classList.toggle('hidden', v !== name));
}

function renderOverview(){
  const el = document.getElementById('view-overview');
  const meetings = (DATA.meetings||[]).slice().sort(byDateDesc);
  const upcoming = (DATA.calendar||[]).filter(e => (e.date||"") >= todayStr())
                    .sort((a,b)=> (a.date||"").localeCompare(b.date||""));
  const latest = meetings.slice(0,3);
  const tags = {};
  meetings.forEach(m => (m.tags||[]).forEach(t => tags[t]=(tags[t]||0)+1));
  const topTags = Object.entries(tags).sort((a,b)=>b[1]-a[1]).slice(0,8);

  el.innerHTML = `
    <div class="kpi">
      <div class="box"><div class="num">${meetings.length}</div><div class="lbl">登録済み会議・声明</div></div>
      <div class="box"><div class="num">${(DATA.calendar||[]).length}</div><div class="lbl">カレンダー登録イベント</div></div>
      <div class="box"><div class="num">${upcoming.length}</div><div class="lbl">今後の予定</div></div>
    </div>
    <div class="card">
      <h3>直近の予定イベント</h3>
      ${upcoming.slice(0,5).map(e => `<div class="meta-row">${esc(e.date)} ・ <span class="chip">${esc(e.category)}</span> ${esc(e.name_ja)}</div>`).join('') || '<div class="meta-row">予定イベントはありません</div>'}
    </div>
    <div class="card">
      <h3>最新の会議・声明</h3>
      ${latest.map(m => `<div class="meta-row">${esc(m.date)} ・ <span class="chip">${esc(m.type)}</span> ${esc(m.title_ja)}</div>`).join('') || '<div class="meta-row">データがありません</div>'}
    </div>
    <div class="card">
      <h3>注目タグ</h3>
      ${topTags.map(([t,n]) => `<span class="chip">${esc(t)} (${n})</span>`).join('') || '<span class="meta-row">タグなし</span>'}
    </div>`;
}

function init(){
  document.getElementById('last-updated').textContent =
    '最終更新: ' + (DATA.meta && DATA.meta.last_updated ? DATA.meta.last_updated : '—');
  document.getElementById('disclaimer').textContent =
    (DATA.meta && DATA.meta.disclaimer) ? DATA.meta.disclaimer
    : '本ツールの考察は参考情報であり、投資助言ではありません。';
  document.querySelectorAll('nav#tabs button').forEach(b =>
    b.addEventListener('click', () => switchView(b.dataset.view)));
  renderOverview();
  // 後続タスクでここに renderMeetings(); renderCalendar(); renderAnalysis(); を追加
}
init();
</script>
</body>
</html>
```

- [ ] **Step 2: data.js を最新化**

Run: `python build.py`
Expected: `data.js` が存在（Task 2/3 で生成済みなら再生成）。

- [ ] **Step 3: ブラウザで概要を確認**

`index.html` をブラウザで開く（ダブルクリック、または Claude Preview/Chrome MCP で `file://.../index.html` を開く）。
Expected:
- ヘッダに「最終更新: 2026-06-22」。
- KPIに「登録済み会議・声明 2」「カレンダー登録イベント 3」「今後の予定 1」。
- 「直近の予定イベント」に 2026-06-15 のG7首脳会議。
- フッタに免責文。
- タブをクリックすると他セクション（まだ空）に切替わる。

- [ ] **Step 4: Checkpoint（Git使用時のみ）**

```bash
git add index.html
git commit -m "feat: dashboard shell with overview view"
```

---

## Task 5: index.html — 会議・声明ログビュー（フィルタ＋原文トグル）

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `DATA.meetings`, `esc()`, `byDateDesc()`。
- Produces: `renderMeetings()`、`toggleOrig(id)`。`init()` に `renderMeetings()` 呼び出しを追加。

- [ ] **Step 1: renderMeetings を実装**

`index.html` の `renderOverview` 関数の後ろ（`function init(){` の前）に追記:

```javascript
function meetingCard(m){
  const a = m.analysis || {};
  const points = (m.key_points_ja||[]).map(p => `<li>${esc(p)}</li>`).join('');
  return `
    <div class="card" data-type="${esc(m.type)}" data-tags="${esc((m.tags||[]).join(','))}" data-text="${esc((m.title_ja||'')+(m.summary_ja||''))}">
      <h3>${esc(m.title_ja)}</h3>
      <div class="meta-row">${esc(m.date)} ・ <span class="chip">${esc(m.type)}</span> ・ 議長国 ${esc(m.presidency)}</div>
      <div><strong>要約:</strong> ${esc(m.summary_ja)}</div>
      ${points ? `<div style="margin-top:6px"><strong>決定事項:</strong><ul>${points}</ul></div>` : ''}
      <div style="margin-top:6px"><strong>考察</strong>
        <ul>
          <li><strong>為替・金利:</strong> ${esc(a.fx_rates)}</li>
          <li><strong>商品・エネルギー・金:</strong> ${esc(a.commodities)}</li>
          <li><strong>セクター:</strong> ${esc(a.sectors)}</li>
          <li><strong>地政学:</strong> ${esc(a.geopolitics)}</li>
        </ul>
      </div>
      ${m.market_direction ? `<div class="meta-row">方向性メモ: ${esc(m.market_direction)}</div>` : ''}
      <div style="margin-top:6px">${(m.tags||[]).map(t=>`<span class="chip">${esc(t)}</span>`).join('')}</div>
      <div style="margin-top:8px">
        <button onclick="toggleOrig('${esc(m.id)}')">原文・全訳を表示/隠す</button>
        ${m.source_url ? `　<a href="${esc(m.source_url)}" target="_blank" rel="noopener">原文リンク ↗</a>` : ''}
      </div>
      <div id="orig-${esc(m.id)}" class="hidden" style="margin-top:8px; border-top:1px dashed var(--line); padding-top:8px">
        <div class="meta-row">原題: ${esc(m.title_orig)}（原語: ${esc(m.source_lang)}）</div>
        <div><strong>主要部分の全訳:</strong><br>${esc(m.full_translation_ja).replace(/\n/g,'<br>')}</div>
      </div>
    </div>`;
}

function toggleOrig(id){
  const el = document.getElementById('orig-'+id);
  if (el) el.classList.toggle('hidden');
}

function renderMeetings(){
  const el = document.getElementById('view-meetings');
  const meetings = (DATA.meetings||[]).slice().sort(byDateDesc);
  const types = [...new Set(meetings.map(m=>m.type).filter(Boolean))];
  el.innerHTML = `
    <div class="card">
      <input id="m-search" type="text" placeholder="キーワード検索（邦題・要約）" style="padding:8px; width:60%; max-width:360px; background:var(--bg); color:var(--txt); border:1px solid var(--line); border-radius:6px;">
      <select id="m-type" style="padding:8px; background:var(--bg); color:var(--txt); border:1px solid var(--line); border-radius:6px;">
        <option value="">すべての種別</option>
        ${types.map(t=>`<option value="${esc(t)}">${esc(t)}</option>`).join('')}
      </select>
    </div>
    <div id="m-list">${meetings.map(meetingCard).join('')}</div>`;
  const apply = () => {
    const q = (document.getElementById('m-search').value||'').toLowerCase();
    const t = document.getElementById('m-type').value;
    document.querySelectorAll('#m-list .card').forEach(card => {
      const okT = !t || card.dataset.type === t;
      const okQ = !q || (card.dataset.text||'').toLowerCase().includes(q);
      card.style.display = (okT && okQ) ? '' : 'none';
    });
  };
  document.getElementById('m-search').addEventListener('input', apply);
  document.getElementById('m-type').addEventListener('change', apply);
}
```

- [ ] **Step 2: init() に呼び出しを追加**

`init()` 内の `renderOverview();` の直後に追記:

```javascript
  renderMeetings();
```

- [ ] **Step 3: ブラウザで確認**

`index.html` を再読込し「会議・声明ログ」タブを開く。
Expected:
- 2件のサンプル会議カードが日付降順で表示。
- 各カードに要約・決定事項・考察(4観点)・タグ・方向性メモ。
- 「原文・全訳を表示/隠す」で原題と全訳が開閉。
- 種別セレクトで「首脳会議」を選ぶと1件に絞られる。
- 検索に「重要鉱物」と入力すると首脳会議サンプルのみ残る。

- [ ] **Step 4: Checkpoint（Git使用時のみ）**

```bash
git add index.html
git commit -m "feat: meetings log view with filter and original-text toggle"
```

---

## Task 6: index.html — イベントカレンダービュー

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `DATA.calendar`, `esc()`, `todayStr()`。
- Produces: `renderCalendar()`。`init()` に呼び出し追加。

- [ ] **Step 1: renderCalendar を実装**

`renderMeetings` の後ろに追記:

```javascript
function renderCalendar(){
  const el = document.getElementById('view-calendar');
  const events = (DATA.calendar||[]).slice().sort((a,b)=>(a.date||'').localeCompare(b.date||''));
  const cats = [...new Set(events.map(e=>e.category).filter(Boolean))];
  // 月別グルーピング
  const groups = {};
  events.forEach(e => { const k=(e.date||'').slice(0,7); (groups[k]=groups[k]||[]).push(e); });
  const today = todayStr();
  const body = Object.keys(groups).sort().map(month => `
    <div class="card">
      <h3>${esc(month)}</h3>
      ${groups[month].map(e => {
        const past = (e.date||'') < today;
        const color = e.status === '済' || past ? 'var(--muted)' : 'var(--accent)';
        return `<div class="meta-row" data-cat="${esc(e.category)}">
          <span style="color:${color}">●</span> ${esc(e.date)} ・
          <span class="chip">${esc(e.category)}</span> ${esc(e.name_ja)}
          <span style="color:var(--muted)">[${esc(e.status)}]</span>
          ${e.note ? '・'+esc(e.note) : ''}
        </div>`;
      }).join('')}
    </div>`).join('');
  el.innerHTML = `
    <div class="card">
      <select id="c-cat" style="padding:8px; background:var(--bg); color:var(--txt); border:1px solid var(--line); border-radius:6px;">
        <option value="">すべてのカテゴリ</option>
        ${cats.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('')}
      </select>
    </div>
    <div id="c-list">${body || '<div class="card meta-row">イベントがありません</div>'}</div>`;
  document.getElementById('c-cat').addEventListener('change', e => {
    const c = e.target.value;
    document.querySelectorAll('#c-list .meta-row[data-cat]').forEach(row => {
      row.style.display = (!c || row.dataset.cat === c) ? '' : 'none';
    });
  });
}
```

- [ ] **Step 2: init() に呼び出しを追加**

`init()` 内の `renderMeetings();` の直後に追記:

```javascript
  renderCalendar();
```

- [ ] **Step 3: ブラウザで確認**

「イベントカレンダー」タブを開く。
Expected:
- 月別（2025-05, 2025-06, 2026-06）にグルーピング表示。
- 各イベントにカテゴリchipと状態。過去/済は灰色●、今後予定は青●。
- カテゴリセレクトで「G7首脳」を選ぶと首脳会議2件のみ表示。

- [ ] **Step 4: Checkpoint（Git使用時のみ）**

```bash
git add index.html
git commit -m "feat: event calendar view grouped by month"
```

---

## Task 7: index.html — 考察ビュー（4観点×時系列）

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `DATA.meetings`, `esc()`, `byDateDesc()`。
- Produces: `renderAnalysis()`。`init()` に呼び出し追加。

- [ ] **Step 1: renderAnalysis を実装**

`renderCalendar` の後ろに追記:

```javascript
const ASPECTS = [
  ['fx_rates','為替・金利'],
  ['commodities','商品・エネルギー・金'],
  ['sectors','セクター'],
  ['geopolitics','地政学'],
];

function renderAnalysis(){
  const el = document.getElementById('view-analysis');
  const meetings = (DATA.meetings||[]).slice().sort(byDateDesc);
  el.innerHTML = ASPECTS.map(([key,label]) => `
    <div class="card">
      <h3>${esc(label)}</h3>
      ${meetings.filter(m => (m.analysis||{})[key]).map(m => `
        <div class="meta-row" style="margin-bottom:8px">
          <strong>${esc(m.date)}</strong>（${esc(m.type)}・${esc(m.presidency)}）<br>
          ${esc(m.analysis[key])}
        </div>`).join('') || '<div class="meta-row">該当する考察がありません</div>'}
    </div>`).join('');
}
```

- [ ] **Step 2: init() に呼び出しを追加**

`init()` 内の `renderCalendar();` の直後に追記:

```javascript
  renderAnalysis();
```

- [ ] **Step 3: ブラウザで確認**

「考察ビュー」タブを開く。
Expected:
- 4観点（為替・金利／商品・エネルギー・金／セクター／地政学）のカードが並ぶ。
- 各カード内に、該当する会議の考察が日付降順で集約表示される。

- [ ] **Step 4: 全テスト・全ビューの最終確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全 PASS。
ブラウザで4タブすべてが正しく表示されることを確認。

- [ ] **Step 5: Checkpoint（Git使用時のみ）**

```bash
git add index.html
git commit -m "feat: analysis view by 4 investment aspects"
```

---

## Task 8: README.md と 更新手順.md

**Files:**
- Create: `README.md`
- Create: `更新手順.md`

**Interfaces:** なし（ドキュメントのみ）。

- [ ] **Step 1: README.md を作成**

`README.md`:

```markdown
# G7一次情報まとめ・考察ツール

G7（主要7カ国）の財務相・中央銀行総裁会議／首脳会議の公式一次情報を、日本語に翻訳・要約し、投資観点の考察とともに閲覧できるローカルツールです。

## 使い方
1. `index.html` をダブルクリックしてブラウザで開く。
2. タブで「概要 / 会議・声明ログ / イベントカレンダー / 考察ビュー」を切り替え。
3. 過去ログ全体は `G7_一次情報ログ.xlsx`（3シート）で確認・フィルタできます。

## 更新（情報の追加）
Claude に「G7情報を更新して」と頼んでください。Claude が公式ソースを巡回し、
`data.json` に追記して `python build.py` を実行 → ダッシュボードとExcelに反映されます。
詳細は `更新手順.md` を参照。

## ファイル
- `data.json` … データ本体（唯一の編集対象）
- `build.py` … data.json → `data.js` + `G7_一次情報ログ.xlsx` を生成
- `index.html` … ダッシュボード
- `G7_一次情報ログ.xlsx` … Excel全archive（自動生成）

## 免責
本ツールの考察は一次情報をもとにした参考情報であり、投資助言・売買推奨ではありません。
最終的な投資判断はご自身の責任で行ってください。
```

- [ ] **Step 2: 更新手順.md を作成**

`更新手順.md`:

```markdown
# 更新手順（Claude向け運用メモ）

## フロー
1. 対象公式ソースを巡回し、新規/未登録の声明・コミュニケを探す。
2. 各件について以下を作成し `data.json` の `meetings[]` に追記:
   - 日付 / 種別 / 議長国 / 原題 / 邦題 / 原文URL / 原語
   - 要約(和) / 決定事項(和) / 主要部分の全訳(和)
   - 考察4観点（為替金利・商品・セクター・地政学）/ 方向性メモ / タグ
3. 関連イベントを `calendar[]` に追記（G7＋G20/FOMC/日銀/ECB等）。状態は予定/済。
4. `meta.last_updated` を更新。
5. `python build.py` を実行 → `data.js` と `G7_一次情報ログ.xlsx` を再生成。
6. 出典が確認できない情報は記載しない（推測で埋めない）。

## 対象公式ソース（一次情報）
- G7議長国サイト（年ごとに変わる。例: 2025カナダ、2026フランス）
- 各国財務省: 米国財務省 home.treasury.gov / 英 HM Treasury gov.uk / 日本 財務省 mof.go.jp・外務省 mofa.go.jp / 独・仏・伊・加 財務省
- EU: 欧州理事会 consilium.europa.eu / 欧州委員会 ec.europa.eu
- 中央銀行: FRB federalreserve.gov / 日銀 boj.or.jp / ECB ecb.europa.eu

## 定期実行（任意）
Claude Code の /loop や scheduled task で「G7情報を更新して」を定期実行することも可能。
```

- [ ] **Step 3: Checkpoint（Git使用時のみ）**

```bash
git add README.md 更新手順.md
git commit -m "docs: add README and update procedure"
```

---

## Task 9: 初期データ投入（直近3〜5年）＋ 最終検証

**Files:**
- Modify: `data.json`（サンプルを実データに置換）

**Interfaces:**
- Consumes: 全タスクの成果物。
- Produces: 実データを反映した `data.js` / `G7_一次情報ログ.xlsx`。

> このタスクは Claude が WebSearch/WebFetch で公式一次情報を収集し、翻訳・考察して `data.json` を作る。出典のない情報は載せない。

- [ ] **Step 1: 公式一次情報を収集**

対象（直近3〜5年、概ね2021/2022〜2026）:
- G7首脳会議コミュニケ／首脳声明（各年）
- G7財務相・中央銀行総裁会議の声明（各回）
収集元は `更新手順.md` の公式ソース一覧。各件で原文URL・原語を必ず記録。
Expected: 主要な会議・声明のURLと原文（または公式要旨）が手元に揃う。

- [ ] **Step 2: 翻訳・要約・考察を作成し data.json を更新**

各件について `meetings[]` のスキーマ全項目を埋める（邦題・要約・決定事項・主要部分の全訳・考察4観点・方向性メモ・タグ）。
`calendar[]` に各会議＋主要マクロイベント（G20/FOMC/日銀/ECB等、判明分）を追記。
`meta.last_updated` を当日に更新。サンプル2件は削除。
Expected: `data.json` が実データのみで構成される。

- [ ] **Step 3: JSON妥当性を確認**

Run: `python -c "import json; d=json.load(open('data.json',encoding='utf-8')); print(len(d['meetings']),'meetings', len(d['calendar']),'events')"`
Expected: 会議件数・イベント件数が想定どおり（複数年分）。エラーなく読める。

- [ ] **Step 4: ビルドして生成物を確認**

Run: `python build.py`
Expected: `OK: N meetings, M calendar events`。`data.js` と `G7_一次情報ログ.xlsx` が更新される。

- [ ] **Step 5: テストとブラウザ最終確認**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全 PASS（テストは固定SAMPLEを使うため実データ投入の影響を受けない）。
ブラウザで `index.html` を開き、4ビューに実データが表示され、原文リンクが実在URLに飛ぶことを確認。
Excel `G7_一次情報ログ.xlsx` の3シートに全ログが入り、原文URLがハイパーリンクになっていることを確認。

- [ ] **Step 6: Checkpoint（Git使用時のみ）**

```bash
git add data.json data.js G7_一次情報ログ.xlsx
git commit -m "data: initial population of G7 primary sources (last 3-5 years)"
```

---

## 完了の定義（受け入れ基準）

- [ ] `index.html` をダブルクリックで開き、概要・会議ログ・カレンダー・考察の4ビューが表示される。
- [ ] 会議ログで日本語要約・決定事項・全訳・4観点考察・原文リンクが見られ、原文トグルが動く。
- [ ] カレンダーにG7＋主要マクロイベントが時系列・カテゴリ別で表示される。
- [ ] `G7_一次情報ログ.xlsx` に全ログが3シートで出力され、フィルタできる（原文URLはハイパーリンク）。
- [ ] 直近3〜5年分の主要なG7会議/声明が初期投入されている。
- [ ] 画面・Excel・README に免責が明記されている。
- [ ] `python -m pytest` が全PASS。
