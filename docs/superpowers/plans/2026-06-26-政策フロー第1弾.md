# 政策→フロー 第1弾 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox (`- [ ]`).

**Goal:** グローバル流動性（ドル換算）・金利差3系列・ブリーフィングの政策予定行を追加し、「政策→資金の流れ」の背骨を作る。

**Architecture:** fetch_macro の computed に `global_liq`（Fed+ECB+日銀のドル換算合算）と汎用 `spread`（FRED 2系列の差）を追加。build_briefing に events（今後14日の calendar 予定）を渡す。マネーの流れビュー先頭に世界流動性の大チャート。

**Tech Stack:** Python 3.12（標準ライブラリ）、Vanilla JS、pytest。新規依存なし。

## Global Constraints
- 配置ルート相対パス。UTF-8。値はFRED公式のみ。生成物は手編集しない。
- 世界流動性: `兆ドル = (WALCL + ECBASSETSW×EURUSD + JPNASSETS×100÷USDJPY) / 1e6`、WALCL日付に整列（他は `_nearest_prior`）、4桁丸め。検算値: WALCL 6,735,645 / ECB 6,119,940×1.147 / BOJ 6,643,630×100÷161.37 → **17.8722兆ドル**。
- spread: minuend日付に整列し `minuend − subtrahend`（4桁丸め）。
- ブリーフィング予定行: `as_of <= date <= as_of+14日`、日付順、最大6件、「データ鮮度」行の前。0件なら行なし。
- 既存テストを壊さない。Gitはmain、各タスク末尾でコミット。
- 既存 fetch_macro: `parse_observations/fetch_series/_nearest_prior/compute_netliq/fetch_netliq/_deflate/fetch_real_m2/fetch_real_netliq/computed_dispatch/get_rows/run`。既存 build: `build_briefing(series,signals,as_of)/build_macro_payload(config_path,data_dir,points)/write_briefing_md/main`。

---

## File Structure
| パス | 区分 | 責務 |
|---|---|---|
| `fetch_macro.py` | 変更 | `fetch_global_liq`/`fetch_spread`、computed_dispatch拡張 |
| `tests/test_fetch_macro.py` | 変更 | global_liq・spreadのテスト |
| `build.py` | 変更 | `build_briefing(events)`、`build_macro_payload(calendar)`、main配線 |
| `tests/test_build.py` | 変更 | briefing events のテスト |
| `series_config.json` | 変更 | GLOBAL_LIQ_USD＋金利差3系列 |
| `index.html` | 変更 | マネーの流れ先頭に世界流動性カード |

---

## Task 1: fetch_global_liq（世界流動性ドル換算）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- Produces: `fetch_global_liq(api_key, fred_fetcher=fetch_series) -> list[tuple[str,float]]`、`computed_dispatch` に `global_liq`。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_fetch_global_liq_converts_and_aligns():
    def fred(sid, key):
        data = {
            "WALCL": [{"date": "2026-06-17", "value": "6700000"},
                       {"date": "2026-06-24", "value": "6735645"}],
            "ECBASSETSW": [{"date": "2026-06-19", "value": "6119940"}],
            "JPNASSETS": [{"date": "2026-05-01", "value": "6643630"}],
            "DEXUSEU": [{"date": "2026-06-18", "value": "1.147"}],
            "DEXJPUS": [{"date": "2026-06-18", "value": "161.37"}],
        }[sid]
        return {"observations": data}
    rows = fetch_macro.fetch_global_liq("key", fred_fetcher=fred)
    # 2026-06-17 は ECB/EURUSD/USDJPY の直近以前値が無く除外。06-24 のみ。
    assert len(rows) == 1
    d, v = rows[0]
    assert d == "2026-06-24"
    assert abs(v - 17.8722) < 0.001   # 検算値


def test_computed_dispatch_global_liq():
    import fetch_macro as fm
    called = {}
    def fake(k, fred_fetcher=None):
        called["ok"] = True
        return [("2026-06-24", 17.87)]
    orig = fm.fetch_global_liq
    fm.fetch_global_liq = fake
    try:
        out = fm.computed_dispatch({"compute": "global_liq"}, "key")
        assert called.get("ok") and out == [("2026-06-24", 17.87)]
    finally:
        fm.fetch_global_liq = orig
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k global_liq -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の `fetch_real_netliq` の後ろに追記:

```python
def fetch_global_liq(api_key, fred_fetcher=fetch_series) -> list:
    """Fed+ECB+日銀の総資産をドル換算合算（兆ドル）。WALCL日付に整列。"""
    walcl = parse_observations(fred_fetcher("WALCL", api_key))          # 百万ドル
    ecb = sorted(parse_observations(fred_fetcher("ECBASSETSW", api_key)))   # 百万ユーロ
    boj = sorted(parse_observations(fred_fetcher("JPNASSETS", api_key)))    # 億円
    eurusd = sorted(parse_observations(fred_fetcher("DEXUSEU", api_key)))
    usdjpy = sorted(parse_observations(fred_fetcher("DEXJPUS", api_key)))
    out = []
    for d, w in sorted(walcl):
        e = _nearest_prior(ecb, d)
        j = _nearest_prior(boj, d)
        fx_e = _nearest_prior(eurusd, d)
        fx_j = _nearest_prior(usdjpy, d)
        if None in (e, j, fx_e, fx_j) or not fx_j:
            continue
        total_mln = w + e * fx_e + j * 100.0 / fx_j
        out.append((d, round(total_mln / 1e6, 4)))
    return out
```

`computed_dispatch` の `if c == "real_netliq":` ブロックの後ろに追記:

```python
    if c == "global_liq":
        return fetch_global_liq(api_key, fred_fetcher)
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k "global_liq" -v`
Expected: 2件 PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: global liquidity index (Fed+ECB+BOJ in USD)"
```

---

## Task 2: fetch_spread（汎用金利差）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- Produces: `fetch_spread(minuend_id, subtrahend_id, api_key, fred_fetcher=fetch_series) -> list`、`computed_dispatch` に `spread`（config の `minuend`/`subtrahend`）。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_fetch_spread_aligns_and_subtracts():
    def fred(sid, key):
        data = {
            "DGS10": [{"date": "2026-06-17", "value": "4.5"},
                      {"date": "2026-06-18", "value": "4.46"}],
            "IRLTLT01JPM156N": [{"date": "2026-05-01", "value": "2.65"}],
        }[sid]
        return {"observations": data}
    rows = fetch_macro.fetch_spread("DGS10", "IRLTLT01JPM156N", "key", fred_fetcher=fred)
    assert rows == [("2026-06-17", 1.85), ("2026-06-18", 1.81)]


def test_computed_dispatch_spread():
    def fred(sid, key):
        data = {"A": [{"date": "2026-06-18", "value": "4.0"}],
                "B": [{"date": "2026-06-18", "value": "2.5"}]}[sid]
        return {"observations": data}
    out = fetch_macro.computed_dispatch(
        {"compute": "spread", "minuend": "A", "subtrahend": "B"}, "key", fred_fetcher=fred)
    assert out == [("2026-06-18", 1.5)]
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k spread -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_global_liq` の後ろに追記:

```python
def fetch_spread(minuend_id: str, subtrahend_id: str, api_key, fred_fetcher=fetch_series) -> list:
    a = parse_observations(fred_fetcher(minuend_id, api_key))
    b = sorted(parse_observations(fred_fetcher(subtrahend_id, api_key)))
    out = []
    for d, v in sorted(a):
        s = _nearest_prior(b, d)
        if s is None:
            continue
        out.append((d, round(v - s, 4)))
    return out
```

`computed_dispatch` の `if c == "global_liq":` ブロックの後ろに追記:

```python
    if c == "spread":
        return fetch_spread(series["minuend"], series["subtrahend"], api_key, fred_fetcher)
```

- [ ] **Step 4: 成功を確認（全fetchテスト）**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: generic FRED spread (rate differentials)"
```

---

## Task 3: ブリーフィングに今後の政策・会議予定

**Files:** Modify `build.py`, `tests/test_build.py`

**Interfaces:**
- `build_briefing(series, signals, as_of, events=None) -> dict`（events=calendarリスト。14日以内・最大6件・鮮度行の前）
- `build_macro_payload(config_path=None, data_dir=None, points=None, calendar=None)`
- `main()` が calendar を渡す。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` 末尾に追記:

```python
def test_build_briefing_upcoming_events():
    signals = {"regime": {"label": "中立", "score": 0.0, "level": "ok"}, "movers": []}
    events = [
        {"date": "2026-06-30", "name_ja": "FOMC", "category": "FOMC"},
        {"date": "2026-07-05", "name_ja": "日銀会合", "category": "日銀"},
        {"date": "2026-08-01", "name_ja": "遠い予定", "category": "G7財務"},
        {"date": "2026-06-20", "name_ja": "過去", "category": "G7首脳"},
    ]
    b = build.build_briefing([], signals, "2026-06-26", events=events)
    text = " ".join(b["lines"])
    assert "今後の政策・会議予定" in text
    assert "06/30 FOMC(FOMC)" in text and "07/05 日銀会合(日銀)" in text
    assert "遠い予定" not in text and "過去" not in text
    # 予定行は鮮度行より前
    idx_ev = next(i for i, l in enumerate(b["lines"]) if "今後の政策・会議予定" in l)
    idx_st = next(i for i, l in enumerate(b["lines"]) if "データ鮮度" in l)
    assert idx_ev < idx_st
    # events無しなら行なし
    b2 = build.build_briefing([], signals, "2026-06-26")
    assert all("今後の政策・会議予定" not in l for l in b2["lines"])
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_build.py -k upcoming -v`
Expected: FAIL（events 未対応）

- [ ] **Step 3: 実装を変更**

`build.py` の `build_briefing` のシグネチャを変更:

```python
def build_briefing(series: list, signals: dict, as_of: str, events=None) -> dict:
```

同関数内、`stale = [s for s in series if s.get("stale")]` の**直前**に追記:

```python
    if events:
        limit = (date.fromisoformat(as_of) + timedelta(days=14)).isoformat()
        upcoming = sorted([e for e in events
                           if as_of <= (e.get("date") or "") <= limit],
                          key=lambda e: e["date"])[:6]
        if upcoming:
            parts = [f"{e['date'][5:7]}/{e['date'][8:10]} {e.get('name_ja','')}({e.get('category','')})"
                     for e in upcoming]
            lines.append("今後の政策・会議予定: " + " / ".join(parts))
```

`build_macro_payload` のシグネチャを変更:

```python
def build_macro_payload(config_path=None, data_dir=None, points=None, calendar=None) -> dict:
```

同関数の `briefing = build_briefing(series, signals, date.today().isoformat())` を次に置換:

```python
    briefing = build_briefing(series, signals, date.today().isoformat(), events=calendar)
```

`main()` の `data["macro"] = build_macro_payload()` を次に置換:

```python
    data["macro"] = build_macro_payload(calendar=data.get("calendar"))
```

- [ ] **Step 4: 成功を確認（全buildテスト）**

Run: `python -m pytest tests/test_build.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add build.py tests/test_build.py
git commit -m "feat: briefing lists upcoming policy events (14 days)"
```

---

## Task 4: 設定追加＋実データ取得

**Files:** Modify `series_config.json`

- [ ] **Step 1: series_config.json に4系列を追加**

`series` 配列内、`REAL_NETLIQ_US` の行の直後に追記:

```json
    { "id": "GLOBAL_LIQ_USD", "country": "世界流動性(ドル換算)", "indicator": "流動性", "unit": "兆ドル", "transform": "yoy_pct_also", "source": "computed", "compute": "global_liq", "group": "market", "risk_dir": "low" },
    { "id": "SPREAD_UST10_JGB10", "country": "日米10年金利差", "indicator": "金利差", "unit": "%", "transform": "level", "source": "computed", "compute": "spread", "minuend": "DGS10", "subtrahend": "IRLTLT01JPM156N", "group": "market" },
    { "id": "SPREAD_UST10_BUND10", "country": "米欧10年金利差", "indicator": "金利差", "unit": "%", "transform": "level", "source": "computed", "compute": "spread", "minuend": "DGS10", "subtrahend": "IRLTLT01DEM156N", "group": "market" },
    { "id": "SPREAD_FF_ECB", "country": "米欧政策金利差", "indicator": "金利差", "unit": "%", "transform": "level", "source": "computed", "compute": "spread", "minuend": "FEDFUNDS", "subtrahend": "ECBDFR", "group": "market" },
```

- [ ] **Step 2: JSON妥当性**

Run: `python -c "import json; print(len(json.load(open('series_config.json',encoding='utf-8'))['series']),'series')"`
Expected: `49 series`

- [ ] **Step 3: 実データ取得と確認**

Run: `python fetch_macro.py`
Expected: `OK: 49 series 取得`。
Run: `python -c "import build; p=build.build_macro_payload(); xs=[s for s in p['series'] if s['id'] in ('GLOBAL_LIQ_USD','SPREAD_UST10_JGB10','SPREAD_UST10_BUND10','SPREAD_FF_ECB')]; [print(s['country'], s['latest_date'], s['latest'], 'z', s['zscore']) for s in xs]"`
Expected: 世界流動性 ≈17.87兆ドル、日米10年金利差 ≈1.8%等が表示。

- [ ] **Step 4: ビルドしてCommit**

Run: `python build.py`
Expected: `OK: ...`。ブリーフィングに「今後の政策・会議予定」行（FOMC 7/28等が14日以内に入る場合）。

```bash
git add series_config.json macro_data "週次ブリーフィング.md" data.js
git commit -m "data: global liquidity + rate differentials series"
```

---

## Task 5: マネーの流れ先頭に世界流動性カード＋最終検証

**Files:** Modify `index.html`

**Interfaces:** `renderMoneyFlow` 内で id `GLOBAL_LIQ_USD` を検索しカード生成。

- [ ] **Step 1: renderMoneyFlow に世界流動性カードを追加**

`index.html` の `renderMoneyFlow` 内、`const analysisCard = ...` の**直前**に追記:

```javascript
  const gl = macro.find(s => s.id === 'GLOBAL_LIQ_USD');
  const glCard = gl ? `
    <div class="card">
      <h3>世界流動性（ドル換算） ── Fed＋ECB＋日銀 総資産</h3>
      <div><span style="font-size:22px;font-weight:700">${esc(gl.latest)}</span> <span class="meta-row" style="display:inline">兆ドル</span>　<span class="meta-row" style="display:inline">前年比 ${esc(fmtYoy(gl.yoy))}</span></div>
      ${lineChartSVG([{points:gl.history}], {h:140})}
      ${chartCaption([{points:gl.history}])}
      <div class="meta-row">※ドル換算のため為替変動（円安・ユーロ安）の影響を含む</div>
    </div>` : '';
```

同関数内の `el.innerHTML = \`` 直後の `${analysisCard}` を `${glCard}${analysisCard}` に置換。

- [ ] **Step 2: ブラウザ確認**

`g7-static` 起動、`index.html` 再読込。
Expected:
- 「マネーの流れ」先頭に世界流動性の大チャート（≈17.9兆ドル・前年比・注意書き・ホバー可）。
- 「市場・リスク」の「流動性」に世界流動性カード、「金利差」テーマに3カード（Zスコア付き）。
- 概要ブリーフィングに「今後の政策・会議予定」行（14日以内の予定があれば）。
- コンソールエラーなし。

- [ ] **Step 3: 全テスト確認**

Run: `python -m pytest -q`
Expected: 全PASS。

- [ ] **Step 4: Commit＆push**

```bash
git add index.html
git commit -m "feat: global liquidity headline chart on money-flow view"
git push
```

---

## 完了の定義
- [ ] 世界流動性（ドル換算）がマネーの流れ先頭＋流動性テーマに表示（検算値と整合）。
- [ ] 金利差3系列がZスコア付きで表示。
- [ ] ブリーフィングに今後14日の政策予定行。
- [ ] `python -m pytest` 全PASS。push後、CI/Pagesにも自動反映。
