# データ拡充（サブB）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox (`- [ ]`).

**Goal:** クロスアセット価格（FRED）・CFTC建玉（新ソースcftc）・実質系（computed）を追加し、既存の市場・リスク／シグナル枠組みに自動統合する。

**Architecture:** fetch_macro に `cftc` ソースと `real_m2/real_netliq` の computed を追加。各 fetcher は共通行形式 `[(YYYY-MM-DD, float)]` を返すので build/index/Excel は無変更で自動反映。

**Tech Stack:** Python 3.12（urllib・json）、pytest。新規依存なし。

## Global Constraints
- 配置ルート相対パス。UTF-8。値は公式データ（FRED/CFTC）のみ。
- CFTC: `https://publicreporting.cftc.gov/resource/6dca-aqww.json`（Socrata, 無料・キー不要）。投機ネット＝`noncomm_positions_long_all − short`。日付は先頭10桁。
- 実質: `名目 ÷ (CPIAUCSL/100)`。CPIは `_nearest_prior` で整列。CPI=0は除外。
- 既存テスト（fred/ecb/boj/computed/manual）を壊さない。Gitはmain、各タスク末尾でコミット。
- 既存 fetch_macro: `parse_observations/fetch_series/fetch_ecb_series/fetch_boj_m2/_nearest_prior/compute_netliq/fetch_netliq/computed_dispatch/get_rows/run`。

---

## File Structure
| パス | 区分 | 責務 |
|---|---|---|
| `fetch_macro.py` | 変更 | `fetch_cftc`/`_parse_cftc_rows`、`_deflate`/`fetch_real_m2`/`fetch_real_netliq`、computed/cftc 配線 |
| `tests/test_fetch_macro.py` | 変更 | CFTC・実質・配線のテスト |
| `series_config.json` | 変更 | B5(3)＋B6(7)＋B7(2) を追加 |

---

## Task 1: CFTC建玉の取得（fetch_cftc）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- `_parse_cftc_rows(records: list[dict]) -> list[tuple[str,float]]`（net＝long−short、日付先頭10桁、昇順）
- `fetch_cftc(market_name: str, urlopen=urllib.request.urlopen) -> list`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_parse_cftc_rows_net_and_sort():
    recs = [
        {"report_date_as_yyyy_mm_dd": "2026-06-16T00:00:00.000",
         "noncomm_positions_long_all": "250000", "noncomm_positions_short_all": "69780"},
        {"report_date_as_yyyy_mm_dd": "2026-06-09T00:00:00.000",
         "noncomm_positions_long_all": "240000", "noncomm_positions_short_all": "60000"},
    ]
    out = fetch_macro._parse_cftc_rows(recs)
    assert out == [("2026-06-09", 180000.0), ("2026-06-16", 180220.0)]


def test_fetch_cftc_builds_query_and_parses():
    import urllib.parse
    captured = {}
    body = ('[{"report_date_as_yyyy_mm_dd":"2026-06-16T00:00:00.000",'
            '"noncomm_positions_long_all":"250000","noncomm_positions_short_all":"69780"}]')
    class _R:
        def read(self): return body.encode("utf-8")
        def __enter__(self): return self
        def __exit__(self, *a): return False
    def fake_urlopen(req, timeout=60):
        captured["url"] = req.full_url
        return _R()
    rows = fetch_macro.fetch_cftc("GOLD - COMMODITY EXCHANGE INC.", urlopen=fake_urlopen)
    assert "publicreporting.cftc.gov" in captured["url"]
    assert "GOLD" in urllib.parse.unquote(captured["url"])
    assert rows == [("2026-06-16", 180220.0)]
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k cftc -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の定数 `ECB_URL = ...` 付近に追記:

```python
CFTC_URL = "https://publicreporting.cftc.gov/resource/6dca-aqww.json"
```

`fetch_boj_m2` の後ろ（`_nearest_prior` の前）に追記:

```python
def _parse_cftc_rows(records: list) -> list:
    out = []
    for r in records:
        d = (r.get("report_date_as_yyyy_mm_dd") or "")[:10]
        if not d:
            continue
        try:
            net = float(r.get("noncomm_positions_long_all")) - float(r.get("noncomm_positions_short_all"))
        except (TypeError, ValueError):
            continue
        out.append((d, net))
    out.sort(key=lambda x: x[0])
    return out


def fetch_cftc(market_name: str, urlopen=urllib.request.urlopen) -> list:
    params = {
        "$select": "report_date_as_yyyy_mm_dd,noncomm_positions_long_all,noncomm_positions_short_all",
        "$where": f"market_and_exchange_names='{market_name}'",
        "$order": "report_date_as_yyyy_mm_dd ASC",
        "$limit": "20000",
    }
    url = CFTC_URL + "?" + urllib.parse.urlencode(params)
    req = urllib.request.Request(url, headers={"User-Agent": "Mozilla/5.0", "Accept": "application/json"})
    with urlopen(req, timeout=60) as r:
        records = json.loads(r.read().decode("utf-8"))
    return _parse_cftc_rows(records)
```

- [ ] **Step 4: 成功を確認 + 実APIチェック**

Run: `python -m pytest tests/test_fetch_macro.py -k cftc -v`
Expected: 2件 PASS。
Run: `python -c "import fetch_macro as m; r=m.fetch_cftc('GOLD - COMMODITY EXCHANGE INC.'); print('rows',len(r),'last',r[-1])"`
Expected: `rows` 多数、`last` が直近週（例 `('2026-06-16', 180220.0)`）。

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: CFTC COT fetcher (speculative net positioning)"
```

---

## Task 2: 実質系（fetch_real_m2 / fetch_real_netliq）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- `_deflate(nominal: list, cpi: list) -> list`（nominal日付に整列、`val/(cpi/100)`、4桁丸め）
- `fetch_real_m2(api_key, fred_fetcher=fetch_series) -> list`
- `fetch_real_netliq(api_key, fred_fetcher=fetch_series) -> list`
- `computed_dispatch` に `real_m2`/`real_netliq` 追加。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_deflate_real():
    nominal = [("2026-03-01", 21000.0), ("2026-04-01", 22000.0)]
    cpi = [("2026-03-01", 310.0), ("2026-04-01", 320.0)]
    out = fetch_macro._deflate(nominal, cpi)
    # 22000/(320/100)=6875.0 ; 21000/(310/100)=6774.1935→6774.1935
    assert out[-1] == ("2026-04-01", 6875.0)


def test_fetch_real_m2_uses_m2_and_cpi():
    def fred(sid, key):
        data = {"M2SL": [{"date": "2026-04-01", "value": "22000"}],
                "CPIAUCSL": [{"date": "2026-04-01", "value": "320"}]}[sid]
        return {"observations": data}
    rows = fetch_macro.fetch_real_m2("key", fred_fetcher=fred)
    assert rows == [("2026-04-01", 6875.0)]
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k "deflate or real_m2" -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の `fetch_netliq` の後ろ・`computed_dispatch` の前に追記:

```python
def _deflate(nominal: list, cpi: list) -> list:
    cpi_s = sorted(cpi)
    out = []
    for d, v in sorted(nominal):
        c = _nearest_prior(cpi_s, d)
        if c is None or c == 0:
            continue
        out.append((d, round(v / (c / 100.0), 4)))
    return out


def fetch_real_m2(api_key, fred_fetcher=fetch_series) -> list:
    m2 = parse_observations(fred_fetcher("M2SL", api_key))
    cpi = parse_observations(fred_fetcher("CPIAUCSL", api_key))
    return _deflate(m2, cpi)


def fetch_real_netliq(api_key, fred_fetcher=fetch_series) -> list:
    netliq = fetch_netliq(api_key, fred_fetcher)
    cpi = parse_observations(fred_fetcher("CPIAUCSL", api_key))
    return _deflate(netliq, cpi)
```

`computed_dispatch` を次に置換:

```python
def computed_dispatch(series: dict, api_key: str, fred_fetcher=fetch_series) -> list:
    c = series.get("compute")
    if c == "netliq_us":
        return fetch_netliq(api_key, fred_fetcher)
    if c == "real_m2":
        return fetch_real_m2(api_key, fred_fetcher)
    if c == "real_netliq":
        return fetch_real_netliq(api_key, fred_fetcher)
    raise ValueError(f"unknown compute: {c}")
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k "deflate or real_m2" -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: real (CPI-adjusted) M2 and net liquidity"
```

---

## Task 3: cftc ソースの配線（get_rows / run）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- `get_rows(..., cftc_fetcher=None)`（`source=="cftc"`→`cftc_fetcher(series["cftc_market"])`）
- `run(..., cftc_fetcher=None, ...)`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_run_handles_cftc_source(tmp_path):
    cfg = {"series": [{"id": "COT_GOLD", "source": "cftc",
                       "cftc_market": "GOLD - X", "transform": "level"}]}
    def fake_cftc(market):
        return [("2026-06-09", 180000.0), ("2026-06-16", 180220.0)]
    res = fetch_macro.run(cfg, "key", cftc_fetcher=fake_cftc, data_dir=tmp_path)
    assert res["ok"] == ["COT_GOLD"]
    txt = (tmp_path / "COT_GOLD.csv").read_text(encoding="utf-8")
    assert "180220" in txt
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k cftc_source -v`
Expected: FAIL（run が cftc_fetcher 未対応）

- [ ] **Step 3: 実装を変更**

`get_rows` を次に置換:

```python
def get_rows(series: dict, api_key: str, *, fred_fetcher, ecb_fetcher, boj_fetcher=None, computed_fetcher=None, cftc_fetcher=None) -> list:
    src = series.get("source")
    if src == "ecb":
        return ecb_fetcher(series["ecb_key"])
    if src == "boj":
        return boj_fetcher()
    if src == "cftc":
        return (cftc_fetcher or fetch_cftc)(series["cftc_market"])
    if src == "computed":
        return (computed_fetcher or computed_dispatch)(series, api_key, fred_fetcher)
    return parse_observations(fred_fetcher(series["id"], api_key))
```

`run` のシグネチャに `cftc_fetcher=None` を追加（`computed_fetcher=None,` の後）:

```python
def run(config: dict, api_key: str, fetcher=fetch_series, ecb_fetcher=None, boj_fetcher=None, computed_fetcher=None, cftc_fetcher=None, data_dir: Path = MACRO_DIR) -> dict:
```

`run` 内の `get_rows(...)` 呼び出しを次に置換:

```python
            rows = get_rows(s, api_key, fred_fetcher=fetcher, ecb_fetcher=ecb_fetcher, boj_fetcher=boj_fetcher, computed_fetcher=computed_fetcher, cftc_fetcher=cftc_fetcher)
```

- [ ] **Step 4: 全テスト確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: wire cftc source into dispatch and run"
```

---

## Task 4: CFTC正式名の確定＋series_config追加＋実取得

**Files:** Modify `series_config.json`

- [ ] **Step 1: CFTC正式市場名を確定**

Run:
```bash
python << 'PY'
import urllib.request, urllib.parse, json
base = "https://publicreporting.cftc.gov/resource/6dca-aqww.json"
for kw in ["GOLD","CRUDE OIL","JAPANESE YEN","EURO FX","DOLLAR INDEX","E-MINI S&P 500","TREASURY NOTES"]:
    p = {"$select":"market_and_exchange_names","$where":f"market_and_exchange_names like '%{kw}%'",
         "$group":"market_and_exchange_names","$limit":"40"}
    url = base + "?" + urllib.parse.urlencode(p)
    req = urllib.request.Request(url, headers={"User-Agent":"Mozilla/5.0"})
    d = json.loads(urllib.request.urlopen(req, timeout=60).read().decode("utf-8"))
    print("==", kw)
    for r in d:
        print("  ", r["market_and_exchange_names"])
PY
```
Expected: 各キーワードの正式名一覧が出る。次の7契約の**正式名**を1つずつ選ぶ（MICRO/E-MINI MICRO/2-YEAR等の別物を除外）:
金=「GOLD - COMMODITY EXCHANGE INC.」、原油=WTI系（例「CRUDE OIL, LIGHT SWEET-WTI - NEW YORK MERCANTILE EXCHANGE」）、円=「JAPANESE YEN - CHICAGO MERCANTILE EXCHANGE」、ユーロ=「EURO FX - CHICAGO MERCANTILE EXCHANGE」、ドル指数=「U.S. DOLLAR INDEX - ICE FUTURES U.S.」系、S&P500=「E-MINI S&P 500 STOCK INDEX - CHICAGO MERCANTILE EXCHANGE」系、米10年=「10-YEAR U.S. TREASURY NOTES - CHICAGO BOARD OF TRADE」系。

- [ ] **Step 2: series_config.json に追加**

`series` 配列内、`NETLIQ_US` の行の直後に追記（`<...正式名...>` は Step 1 で確定した文字列に置換）:

```json
    { "id": "SP500", "country": "S&P500", "indicator": "株価指数", "unit": "pt", "transform": "level", "group": "market" },
    { "id": "NIKKEI225", "country": "日経225", "indicator": "株価指数", "unit": "円", "transform": "level", "group": "market" },
    { "id": "PCOPPUSDM", "country": "銅", "indicator": "商品", "unit": "ドル/トン", "transform": "level", "group": "market" },
    { "id": "COT_GOLD", "country": "金", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "GOLD - COMMODITY EXCHANGE INC." },
    { "id": "COT_WTI", "country": "WTI原油", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "<WTI原油の正式名>" },
    { "id": "COT_JPY", "country": "円", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "JAPANESE YEN - CHICAGO MERCANTILE EXCHANGE" },
    { "id": "COT_EUR", "country": "ユーロ", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "EURO FX - CHICAGO MERCANTILE EXCHANGE" },
    { "id": "COT_USD", "country": "ドル指数", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "<ドル指数の正式名>" },
    { "id": "COT_SPX", "country": "S&P500", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "<E-mini S&P500の正式名>" },
    { "id": "COT_UST10", "country": "米10年", "indicator": "建玉(COT)", "unit": "枚", "transform": "level", "group": "market", "source": "cftc", "cftc_market": "<米10年国債の正式名>" },
    { "id": "REAL_M2_US", "country": "実質M2(米)", "indicator": "実質(インフレ調整)", "unit": "10億ドル(実質)", "transform": "yoy_pct_also", "group": "market", "source": "computed", "compute": "real_m2" },
    { "id": "REAL_NETLIQ_US", "country": "実質ネット流動性", "indicator": "実質(インフレ調整)", "unit": "兆ドル(実質)", "transform": "yoy_pct_also", "group": "market", "source": "computed", "compute": "real_netliq" },
```

- [ ] **Step 3: JSON妥当性**

Run: `python -c "import json; print(len(json.load(open('series_config.json',encoding='utf-8'))['series']),'series')"`
Expected: `47 series`（既存35＋12）

- [ ] **Step 4: 実データ取得**

Run: `python fetch_macro.py`
Expected: `OK: 47 series 取得`（未取得が出たら標準エラー一覧→ Step 1 の正式名を見直し）。`macro_data/` に SP500.csv・COT_*.csv・REAL_*.csv が生成。

- [ ] **Step 5: 追加系列を確認**

Run: `python -c "import build; p=build.build_macro_payload(); xs=[s for s in p['series'] if s['indicator'] in ('建玉(COT)','株価指数','実質(インフレ調整)')]; [print(s['indicator'], s['country'], s['latest_date'], round(s['latest'],1), 'z', s['zscore']) for s in xs]"`
Expected: COT7・株価指数2・実質2 の最新値＋Zスコアが表示される。

- [ ] **Step 6: ビルド**

Run: `python build.py`
Expected: `OK: 7 meetings, 19 calendar events`。

- [ ] **Step 7: ブラウザ確認**

`g7-static` 起動、`index.html` 再読込、「市場・リスク」タブ。
Expected: 「株価指数」「商品(原油＋銅)」「建玉(COT)7契約」「実質(インフレ調整)」が並び、各カードにZスコア・信号色（risk_dir無しのため信号色は出ないがZ/%ileは表示）。コンソールエラーなし。

- [ ] **Step 8: 全テスト確認とCommit**

Run: `python -m pytest -q`
Expected: 全PASS。
```bash
git add series_config.json macro_data
git commit -m "data: add cross-asset prices, CFTC COT (7), real M2/net-liquidity"
```

---

## 完了の定義
- [ ] `python fetch_macro.py` で B5(3)＋B6(7)＋B7(2)＝12系列が取得/計算され保存（`OK: 47 series`）。
- [ ] 「市場・リスク」に株価指数・商品(原油+銅)・建玉(COT)7・実質2 が並ぶ。
- [ ] 各新系列にZスコア・%ile・ムーバーが自動適用。週次タスクで自動更新。
- [ ] `python -m pytest` 全PASS（既存を壊さない）。
