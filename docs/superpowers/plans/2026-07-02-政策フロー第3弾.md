# 政策→フロー 第3弾 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). Steps use checkbox (`- [ ]`).

**Goal:** 政策サイクル自動判定・イベントマーカー・金融政策ログ・銀行信用を追加し、「政策→資金の流れ」ダッシュボードを完成させる。

**Architecture:** build.py に `build_policy_cycle`（政策金利4系列の6か月変化→方向判定）を追加し signals に載せる。index.html に政策サイクルカードとチャートマーカー。金融政策決定は一次情報確認のうえ data.json に種別「金融政策」で追加。

**Tech Stack:** Python 3.12（標準ライブラリ）、Vanilla JS、pytest。新規依存なし。

## Global Constraints
- 方向判定: `delta6m > 0.1`→up(引き締め)、`< -0.1`→down(緩和)、間→flat(据置)。6か月前(183日)以前の値が無い系列はskip。
- 新4系列は `group` なし（fundamental、「マクロ」タブ）。計 55+4=59系列。
- マーカーはチャートx範囲内のみ・最寄りx位置に破線。金融政策ログは公式一次情報のみ（推測なし）。
- 既存テストを壊さない。Gitはmain、各タスク末尾でコミット。
- 既存: `build.build_signals/build_macro_payload/_value_on_or_before`、`index.html` の `lineChartSVG/renderOverview/renderMoneyFlow/esc`。

---

## File Structure
| パス | 区分 | 責務 |
|---|---|---|
| `build.py` | 変更 | `build_policy_cycle`、signalsへの付与 |
| `tests/test_build.py` | 変更 | 方向判定・payloadテスト |
| `series_config.json` | 変更 | 政策金利2＋銀行信用2 |
| `index.html` | 変更 | policyCycleCard、markers、マネーの流れ統合 |
| `data.json` | 変更 | 金融政策ログ（確認済み分） |

---

## Task 1: build_policy_cycle（方向判定）

**Files:** Modify `build.py`, `tests/test_build.py`

**Interfaces:**
- `build_policy_cycle(series: list) -> list[{country, latest, delta6m, dir}]`
- `build_macro_payload` 内で `signals["policy_cycle"]` に格納。

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_build.py` 末尾に追記:

```python
def test_build_policy_cycle_directions():
    def mk(country, latest, prior):
        return {"country": country, "indicator": "政策金利", "latest": latest,
                "latest_date": "2026-06-01",
                "history": [["2025-11-01", prior], ["2026-06-01", latest]]}
    series = [
        mk("米国", 3.63, 4.33),
        mk("日本", 0.75, 0.5),
        mk("ユーロ圏", 2.25, 2.25),
        {"country": "英国", "indicator": "政策金利", "latest": 4.0,
         "latest_date": "2026-06-01", "history": [["2026-06-01", 4.0]]},
        {"country": "x", "indicator": "CPI", "latest": 300.0,
         "latest_date": "2026-06-01", "history": [["2020-01-01", 1.0], ["2026-06-01", 300.0]]},
    ]
    pc = build.build_policy_cycle(series)
    d = {p["country"]: p for p in pc}
    assert d["米国"]["dir"] == "down" and d["米国"]["delta6m"] == -0.7
    assert d["日本"]["dir"] == "up"
    assert d["ユーロ圏"]["dir"] == "flat"
    assert "英国" not in d      # 6か月前なし → skip
    assert "x" not in d         # 政策金利以外は対象外


def test_payload_signals_policy_cycle(tmp_path):
    _write_macro_fixture(tmp_path)
    p = build.build_macro_payload(
        config_path=tmp_path / "series_config.json", data_dir=tmp_path / "macro_data")
    assert "policy_cycle" in p["signals"]
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_build.py -k policy_cycle -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`build.py` の `build_signals` の後ろに追記:

```python
def build_policy_cycle(series: list) -> list:
    out = []
    for s in series:
        if s.get("indicator") != "政策金利":
            continue
        hist = s.get("history") or []
        if not hist:
            continue
        yy, mm, dd = (int(x) for x in s["latest_date"].split("-"))
        target = (date(yy, mm, dd) - timedelta(days=183)).isoformat()
        prior = None
        for d, v in hist:
            if d <= target:
                prior = v
            else:
                break
        if prior is None:
            continue
        delta = round(s["latest"] - prior, 2)
        direction = "up" if delta > 0.1 else ("down" if delta < -0.1 else "flat")
        out.append({"country": s["country"], "latest": s["latest"],
                    "delta6m": delta, "dir": direction})
    return out
```

`build_macro_payload` 内の `signals = build_signals(series)` の直後に追記:

```python
    signals["policy_cycle"] = build_policy_cycle(series)
```

- [ ] **Step 4: 成功を確認**

Run: `python -m pytest tests/test_build.py -k policy_cycle -v`
Expected: 2件 PASS

- [ ] **Step 5: Commit**

```bash
git add build.py tests/test_build.py
git commit -m "feat: policy cycle detection (6-month rate direction)"
```

---

## Task 2: 設定4系列＋実データ取得

**Files:** Modify `series_config.json`

- [ ] **Step 1: series_config.json に追記**

`FDHBFIN` の行の直後に追記:

```json
    { "id": "IRSTCI01JPM156N", "country": "日本", "indicator": "政策金利", "unit": "%", "transform": "level" },
    { "id": "IRSTCI01GBM156N", "country": "英国", "indicator": "政策金利", "unit": "%", "transform": "level" },
    { "id": "TOTBKCR", "country": "米国", "indicator": "銀行信用", "unit": "十億ドル", "transform": "yoy_pct_also" },
    { "id": "BUSLOANS", "country": "米国(C&Iローン)", "indicator": "銀行信用", "unit": "十億ドル", "transform": "yoy_pct_also" },
```

- [ ] **Step 2: JSON妥当性と取得**

Run: `python -c "import json; print(len(json.load(open('series_config.json',encoding='utf-8'))['series']),'series')"`
Expected: `59 series`
Run: `python fetch_macro.py`
Expected: `OK: 59 series 取得`

- [ ] **Step 3: 政策サイクルの実値確認**

Run: `python -c "import build; p=build.build_macro_payload(); [print(x['country'], x['latest'],'%', x['dir'], x['delta6m']) for x in p['signals']['policy_cycle']]"`
Expected: 米国・ユーロ圏・日本・英国の4行（方向付き）。

- [ ] **Step 4: ビルドとCommit**

Run: `python build.py`

```bash
git add series_config.json macro_data data.js "週次ブリーフィング.md"
git commit -m "data: JP/UK policy-rate proxies + US bank credit"
```

---

## Task 3: 政策サイクルカード＋イベントマーカー（index.html）

**Files:** Modify `index.html`

**Interfaces:** `policyCycleCard()`、`lineChartSVG(seriesList, opts)` に `opts.markers`。

- [ ] **Step 1: lineChartSVG に markers を追加**

`index.html` の `lineChartSVG` 内、`const zero = ...;` の直後に追記:

```javascript
  let markerLines = '';
  if(opts.markers && xs.length > 1){
    const seen = new Set();
    opts.markers.forEach(md => {
      if(!md || md < xs[0] || md > xs[xs.length-1]) return;
      let nearest = xs[0];
      for(const x of xs){ if(x <= md) nearest = x; else break; }
      if(seen.has(nearest)) return;
      seen.add(nearest);
      const mx = sx(nearest).toFixed(1);
      markerLines += `<line x1="${mx}" y1="0" x2="${mx}" y2="${H}" stroke="#8a7a4f" stroke-width="0.6" stroke-dasharray="3,3"/>`;
    });
  }
```

同関数内の `const svg = \`<svg viewBox="0 0 ${W} ${H}" width="100%" height="${H}" preserveAspectRatio="none">${zero}${paths}</svg>\`;` を次に置換:

```javascript
  const svg = `<svg viewBox="0 0 ${W} ${H}" width="100%" height="${H}" preserveAspectRatio="none">${markerLines}${zero}${paths}</svg>`;
```

- [ ] **Step 2: policyCycleCard を追加**

`signalBadge` 関数の直後に追記:

```javascript
function policyCycleCard(){
  const pc = (DATA.macro && DATA.macro.signals && DATA.macro.signals.policy_cycle) || [];
  if(!pc.length) return '';
  const arrow = d => d==='up' ? '↑引き締め' : (d==='down' ? '↓緩和' : '→据置');
  const color = d => d==='up' ? '#ff8c4f' : (d==='down' ? '#4fd08c' : '#9fb2cf');
  return `<div class="card">
    <h3>政策サイクル（政策金利・直近6か月の変化）</h3>
    <div style="display:flex; gap:18px; flex-wrap:wrap">
      ${pc.map(p => `<div><div class="meta-row" style="margin:0">${esc(p.country)}</div>
        <span style="font-size:18px;font-weight:700">${esc(p.latest)}%</span>
        <span style="color:${color(p.dir)}"> ${esc(arrow(p.dir))} (${p.delta6m>0?'+':''}${esc(p.delta6m)})</span></div>`).join('')}
    </div>
  </div>`;
}
```

- [ ] **Step 3: 概要とマネーの流れに配置＋マーカー適用**

`renderOverview` 内、週次ブリーフィングカードの `</div>`（ブリーフィングカード終了）の直後に追記:

```javascript
    ${policyCycleCard()}
```

`renderMoneyFlow` 内、`const gl = ...` の**直前**に追記:

```javascript
  const policyCats = new Set(['FOMC','日銀','ECB','G7首脳','G7財務']);
  const evMarkers = (DATA.calendar||[]).filter(e => policyCats.has(e.category)).map(e => e.date);
  const iv = macro.find(s => s.id === 'MOF_INTERVENTION');
  const markers = evMarkers.concat(iv && iv.history ? iv.history.map(p => p[0]) : []);
```

glCard の `${lineChartSVG([{points:gl.history}], {h:140})}` を `${lineChartSVG([{points:gl.history}], {h:140, markers})}` に、
flowCard の `${lineChartSVG(flowLines, {h:140})}` を `${lineChartSVG(flowLines, {h:140, markers})}` に置換。
glCard・flowCard の注記行にそれぞれ `　縦線＝政策イベント・為替介入` を追記（既存 meta-row 文末に追加）。
`el.innerHTML = \`` 直後の `${glCard}` を `${policyCycleCard()}${glCard}` に置換。

- [ ] **Step 4: ブラウザ確認**

`g7-static` 起動、再読込。
Expected:
- 概要: ブリーフィングの下に「政策サイクル」カード（4中銀・方向色付き）。
- マネーの流れ: 先頭に政策サイクル → 世界流動性（縦線マーカー入り）→ 週次フロー（マーカー入り）の因果順。
- マクロ: 「政策金利」に日米欧英4カード、「銀行信用」テーマ2カード。
- コンソールエラーなし。

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "feat: policy-cycle card + event markers on flow charts"
```

---

## Task 4: 金融政策ログ初期投入＋最終検証＋push

**Files:** Modify `data.json`

- [ ] **Step 1: 直近の金融政策決定を一次情報で確認**

WebSearch/WebFetch で FOMC（直近会合）・日銀（直近会合）・ECB（直近理事会）の**公式声明**（federalreserve.gov / boj.or.jp / ecb.europa.eu）を確認。各決定の日付・金利・主要文言を採録（確認できたもののみ）。

- [ ] **Step 2: data.json に種別「金融政策」で追加**

確認できた各決定を `meetings[]` に追加（スキーマは既存どおり: id/date/type="金融政策"/presidency=中銀名/原題/邦題/URL/要約/決定事項/全訳(主要部)/4観点考察/方向性メモ/タグ）。`meta.last_updated` を更新。

- [ ] **Step 3: 検証**

Run: `python -c "import json; d=json.load(open('data.json',encoding='utf-8')); print(len(d['meetings']),'meetings'); [print(m['date'], m['title_ja']) for m in d['meetings'] if m['type']=='金融政策']"`
Expected: 追加分が表示。
Run: `python build.py`
Expected: meetings件数が増えて成功。

- [ ] **Step 4: 全テスト・ブラウザ最終確認・Commit＆push**

Run: `python -m pytest -q`（タイムアウト余裕を持たせて実行）
Expected: 全PASS。
ブラウザ: 会議・声明ログに「金融政策」種別が表示・フィルタ可能。

```bash
git add data.json data.js "週次ブリーフィング.md"
git commit -m "data: recent central-bank policy decisions (FOMC/BOJ/ECB)"
git push
```

---

## 完了の定義
- [ ] 政策金利4中銀・銀行信用2カード（59系列）。
- [ ] 概要・マネーの流れに政策サイクルカード。主要チャートに政策/介入マーカー。
- [ ] 金融政策ログ（確認済み分）が会議ログに表示。
- [ ] `python -m pytest` 全PASS。push後CI/Pagesに自動反映。
