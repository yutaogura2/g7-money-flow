# ECBマルチソース化（ユーロ圏M2/M3追加）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `fetch_macro.py` をマルチソース化（`source: fred|ecb`）し、ECB公式APIでユーロ圏M2・M3を取得して既存のマクロ・ダッシュボード/Excelに統合する。

**Architecture:** 各系列に `source` を持たせ、取得時にソース別fetcherへ振り分ける。ECB fetcherは結果を共通行形式 `[(YYYY-MM-01, float)]` に正規化して返すので、以降のYoY計算・CSV書出・build・表示はソース非依存で流用できる（大半無変更）。

**Tech Stack:** Python 3.12（標準ライブラリ urllib・csv・io、既存 openpyxl）、Vanilla JS、pytest。**新規依存なし。**

## Global Constraints

- 配置ルート: `C:\Users\yutao\OneDrive\デスクトップ\★Claud code★\銀行一次情報`（パスは相対）。
- 文字コードUTF-8。生成物（data.js・xlsx・CSV）は手編集しない。
- ECBエンドポイント: `https://data-api.ecb.europa.eu/service/data/{ecb_key}?format=csvdata`（`Accept: text/csv`、無料・キー不要）。
- ECB CSVは引用符内カンマを含むため **`csv` モジュールで解析**。`TIME_PERIOD`/`OBS_VALUE` 列を使用。日付 `YYYY-MM` → `YYYY-MM-01` に正規化。
- 取得失敗・欠損はスキップして明示（推測で埋めない）。既存テストを壊さない。
- 既存の `fetch_macro` 関数: `load_config/load_api_key/parse_observations/compute_yoy/to_csv_text/write_series_csv/fetch_series/run/main`、定数 `FRED_URL/MACRO_DIR` 等。
- 既存の `index.html` 関数 `renderMoneyFlow` 内のM2比較フィルタを1か所変更。
- Gitはmainブランチ。各タスク末尾でコミット。

---

## File Structure

| パス | 区分 | 責務 |
|---|---|---|
| `fetch_macro.py` | 変更 | ECB CSV解析・取得、source振り分け（get_rows）、run()配線 |
| `tests/test_fetch_macro.py` | 変更 | ECB解析・取得・振り分けのテスト |
| `series_config.json` | 変更 | ECBのユーロ圏M2/M3を追加（source/ecb_key付き） |
| `index.html` | 変更 | マネーの流れのM2比較を完全一致に |

---

## Task 1: ECB CSV解析と取得関数

**Files:**
- Modify: `fetch_macro.py`
- Modify: `tests/test_fetch_macro.py`

**Interfaces:**
- Produces:
  - `_parse_ecb_csv(text: str) -> list[tuple[str, float]]`（`TIME_PERIOD/OBS_VALUE`を読み、欠損除外、`YYYY-MM`→`YYYY-MM-01`正規化、日付昇順）
  - `fetch_ecb_series(ecb_key: str, urlopen=urllib.request.urlopen) -> list[tuple[str, float]]`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` の末尾に追記:

```python
class _FakeResp:
    def __init__(self, text): self._t = text
    def read(self): return self._t.encode("utf-8")
    def __enter__(self): return self
    def __exit__(self, *a): return False


def test_parse_ecb_csv_normalizes_and_handles_quoted_commas():
    text = ('TIME_PERIOD,OBS_VALUE,TITLE_COMPL\n'
            '2026-03,16282398,"M2, stocks"\n'
            '2026-02,16244868,"M2, stocks"\n'
            '2026-04,,"empty value"\n')
    rows = fetch_macro._parse_ecb_csv(text)
    assert rows == [("2026-02-01", 16244868.0), ("2026-03-01", 16282398.0)]


def test_fetch_ecb_series_builds_url_and_parses():
    captured = {}
    def fake_urlopen(req, timeout=30):
        captured["url"] = req.full_url
        return _FakeResp("TIME_PERIOD,OBS_VALUE\n2026-03,16282398\n2026-02,16244868\n")
    rows = fetch_macro.fetch_ecb_series("BSI/M.U2.Y.V.M20.X.1.U2.2300.Z01.E", urlopen=fake_urlopen)
    assert "data-api.ecb.europa.eu/service/data/BSI/M.U2.Y.V.M20.X.1.U2.2300.Z01.E" in captured["url"]
    assert "format=csvdata" in captured["url"]
    assert rows == [("2026-02-01", 16244868.0), ("2026-03-01", 16282398.0)]
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k ecb -v`
Expected: FAIL（`_parse_ecb_csv` / `fetch_ecb_series` 未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の import 群に追記（既存の import 群に並べる）:

```python
import csv
import io
```

定数 `FRED_URL = ...` の直後に追記:

```python
ECB_URL = "https://data-api.ecb.europa.eu/service/data"
```

`fetch_series` 関数の後ろに追記:

```python
def _parse_ecb_csv(text: str) -> list:
    rows = []
    reader = csv.DictReader(io.StringIO(text))
    for row in reader:
        tp = (row.get("TIME_PERIOD") or "").strip()
        ov = (row.get("OBS_VALUE") or "").strip()
        if not tp or ov == "":
            continue
        try:
            v = float(ov)
        except ValueError:
            continue
        if len(tp) == 7:        # YYYY-MM
            d = tp + "-01"
        elif len(tp) == 4:      # YYYY
            d = tp + "-01-01"
        else:                   # YYYY-MM-DD など
            d = tp
        rows.append((d, v))
    rows.sort(key=lambda r: r[0])
    return rows


def fetch_ecb_series(ecb_key: str, urlopen=urllib.request.urlopen) -> list:
    url = f"{ECB_URL}/{ecb_key}?format=csvdata"
    req = urllib.request.Request(url, headers={"Accept": "text/csv"})
    with urlopen(req, timeout=30) as r:
        text = r.read().decode("utf-8")
    return _parse_ecb_csv(text)
```

- [ ] **Step 4: テスト成功を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k ecb -v`
Expected: 2件 PASS

- [ ] **Step 5: 実APIで現行性を確認（任意の手動チェック）**

Run: `python -c "import fetch_macro as m; r=m.fetch_ecb_series('BSI/M.U2.Y.V.M20.X.1.U2.2300.Z01.E'); print('rows',len(r),'last',r[-1][0])"`
Expected: `rows` が数百、`last` が直近月（例 `2026-04-01`）。

- [ ] **Step 6: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: ECB CSV fetcher with date normalization"
```

---

## Task 2: source振り分け（get_rows）と run() 配線

**Files:**
- Modify: `fetch_macro.py`
- Modify: `tests/test_fetch_macro.py`

**Interfaces:**
- Consumes: `parse_observations`, `fetch_series`, `fetch_ecb_series`, `compute_yoy`, `write_series_csv`
- Produces:
  - `get_rows(series: dict, api_key: str, *, fred_fetcher, ecb_fetcher) -> list[tuple[str,float]]`
  - `run(config, api_key, fetcher=fetch_series, ecb_fetcher=None, data_dir=MACRO_DIR)`（`source`で分岐、後方互換）

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` の末尾に追記:

```python
def test_get_rows_dispatches_by_source():
    fred_calls, ecb_calls = [], []
    def fake_fred(sid, key):
        fred_calls.append(sid)
        return {"observations": [{"date": "2020-01-01", "value": "1"}]}
    def fake_ecb(key):
        ecb_calls.append(key)
        return [("2026-04-01", 16289850.0)]
    r_fred = fetch_macro.get_rows({"id": "M2SL"}, "key",
                                  fred_fetcher=fake_fred, ecb_fetcher=fake_ecb)
    r_ecb = fetch_macro.get_rows({"id": "X", "source": "ecb", "ecb_key": "BSI/KEY"}, "key",
                                 fred_fetcher=fake_fred, ecb_fetcher=fake_ecb)
    assert fred_calls == ["M2SL"] and r_fred == [("2020-01-01", 1.0)]
    assert ecb_calls == ["BSI/KEY"] and r_ecb == [("2026-04-01", 16289850.0)]


def test_run_handles_ecb_source(tmp_path):
    cfg = {"series": [{"id": "ECB_M2_EUR", "source": "ecb",
                       "ecb_key": "BSI/KEY", "transform": "yoy_pct_also"}]}
    def fake_ecb(key):
        return [("2025-04-01", 100.0), ("2026-04-01", 110.0)]
    res = fetch_macro.run(cfg, "key", ecb_fetcher=fake_ecb, data_dir=tmp_path)
    assert res["ok"] == ["ECB_M2_EUR"]
    txt = (tmp_path / "ECB_M2_EUR.csv").read_text(encoding="utf-8")
    assert txt.startswith("date,value,yoy_pct")
    assert "10.0" in txt  # YoY +10%
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k "dispatch or ecb_source" -v`
Expected: FAIL（`get_rows` 未定義 / run が ecb_fetcher を受けない）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の `fetch_ecb_series` の後ろに追記:

```python
def get_rows(series: dict, api_key: str, *, fred_fetcher, ecb_fetcher) -> list:
    if series.get("source") == "ecb":
        return ecb_fetcher(series["ecb_key"])
    return parse_observations(fred_fetcher(series["id"], api_key))
```

`run` 関数を次の内容に**置換**:

```python
def run(config: dict, api_key: str, fetcher=fetch_series, ecb_fetcher=None, data_dir: Path = MACRO_DIR) -> dict:
    if ecb_fetcher is None:
        ecb_fetcher = fetch_ecb_series
    ok, failed = [], []
    for s in config.get("series", []):
        sid = s["id"]
        with_yoy = s.get("transform") == "yoy_pct_also"
        try:
            rows = get_rows(s, api_key, fred_fetcher=fetcher, ecb_fetcher=ecb_fetcher)
            if not rows:
                failed.append((sid, "観測値なし"))
                continue
            out = compute_yoy(rows) if with_yoy else rows
            write_series_csv(sid, out, with_yoy, data_dir)
            ok.append(sid)
        except Exception as e:  # noqa: BLE001  個別失敗はスキップして継続
            failed.append((sid, str(e)))
    return {"ok": ok, "failed": failed}
```

- [ ] **Step 4: テスト成功を確認（既存も含め全件）**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS（既存の `test_run_writes_ok_and_skips_failures` も `fetcher=` 引数のままfred経路で通る）

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: source dispatch (fred/ecb) in fetch orchestration"
```

---

## Task 3: 設定にECBユーロM2/M3を追加し実データ取得

**Files:**
- Modify: `series_config.json`

**Interfaces:**
- Consumes: Task 1/2 のECB取得。
- Produces: `macro_data/ECB_M2_EUR.csv`, `macro_data/ECB_M3_EUR.csv` と反映済み生成物。

- [ ] **Step 1: series_config.json にECB 2系列を追加**

`series_config.json` の `series` 配列内、`CP0000EZ19M086NEST`（ユーロ圏CPI）の項目の直後に2要素を追記:

```json
    { "id": "ECB_M2_EUR", "country": "ユーロ圏", "indicator": "マネーサプライ(M2)", "unit": "百万ユーロ", "transform": "yoy_pct_also", "source": "ecb", "ecb_key": "BSI/M.U2.Y.V.M20.X.1.U2.2300.Z01.E" },
    { "id": "ECB_M3_EUR", "country": "ユーロ圏", "indicator": "マネーサプライ(M3)", "unit": "百万ユーロ", "transform": "yoy_pct_also", "source": "ecb", "ecb_key": "BSI/M.U2.Y.V.M30.X.1.U2.2300.Z01.E" },
```

- [ ] **Step 2: JSON妥当性を確認**

Run: `python -c "import json; d=json.load(open('series_config.json',encoding='utf-8')); print(len(d['series']),'series')"`
Expected: `19 series`（既存17＋2）

- [ ] **Step 3: 実データを取得**

Run: `python fetch_macro.py`
Expected: `OK: 19 series 取得`（未取得が出たら標準エラーに表示）。`macro_data/ECB_M2_EUR.csv` と `ECB_M3_EUR.csv` が生成される。

- [ ] **Step 4: 取得内容を確認**

Run: `python -c "import build; p=build.build_macro_payload(); xs=[s for s in p['series'] if s['country']=='ユーロ圏' and s['indicator'].startswith('マネーサプライ')]; [print(s['indicator'], s['latest_date'], s['latest'], s['yoy']) for s in xs]"`
Expected: `マネーサプライ(M2)` と `マネーサプライ(M3)` の2行が直近月（例 2026-04-01）で表示される。

- [ ] **Step 5: ビルドして生成物に反映**

Run: `python build.py`
Expected: `OK: 7 meetings, 19 calendar events`。`data.js` とExcelにユーロ圏M2/M3が入る。

- [ ] **Step 6: Commit**

```bash
git add series_config.json macro_data/ECB_M2_EUR.csv macro_data/ECB_M3_EUR.csv
git commit -m "data: add euro area M2/M3 from ECB API"
```

---

## Task 4: ダッシュボードのM2比較を完全一致に + 最終検証

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `DATA.macro.series`（ユーロ圏M2/M3を含む）。
- Produces: マネーの流れのM2比較が「米国＋ユーロ圏（M2のみ）」になる。

- [ ] **Step 1: M2比較フィルタを完全一致に変更**

`index.html` の `renderMoneyFlow` 内、次の行を置換:

```javascript
  const m2 = macro.filter(s => s.indicator.indexOf('マネーサプライ') === 0);
```

変更後:

```javascript
  const m2 = macro.filter(s => s.indicator === 'マネーサプライ(M2)');
```

- [ ] **Step 2: ブラウザで確認**

`.claude/launch.json` の `g7-static` を起動し `index.html` を開く（または再読込）。
Expected:
- 「マクロ」ビュー: カテゴリ「マネーサプライ(M2)」に **米国＋ユーロ圏** の2カード、新カテゴリ「マネーサプライ(M3)」に **ユーロ圏** のカード。
- 「マネーの流れ」: M2前年比チャートに **米国・ユーロ圏** の2本＋凡例、最新前年比に両国。
- コンソールエラーなし。

- [ ] **Step 3: 全テスト確認**

Run: `python -m pytest -v`
Expected: 全PASS（`test_build.py` + `test_fetch_macro.py`）。

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "feat: money-flow M2 compares US and euro area (exact match)"
```

---

## 完了の定義（受け入れ基準）

- [ ] `python fetch_macro.py` でユーロ圏M2・M3が `macro_data/ECB_M2_EUR.csv` `ECB_M3_EUR.csv` に現行値で保存される。
- [ ] 「マクロ」: 「マネーサプライ(M2)」=米国＋ユーロ圏、「マネーサプライ(M3)」=ユーロ圏。
- [ ] 「マネーの流れ」: M2比較に米国＋ユーロ圏が並ぶ。
- [ ] Excel `マクロ_最新`/`マクロ_時系列` にユーロ圏M2/M3が反映。
- [ ] `python -m pytest` 全PASS（既存を壊さない）。
- [ ] ECB取得は無料・APIキー不要で動作。
