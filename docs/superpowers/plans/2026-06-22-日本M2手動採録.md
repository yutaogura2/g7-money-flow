# 日本M2 手動採録ソース 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 日本M2を `source: "manual"` で追加し、`fetch_macro.py` が手動ソースをスキップ（自動取得・上書きしない）するようにして、Claudeが日銀公表値を採録した `macro_data/JP_M2_MANUAL.csv` をダッシュボード/Excelに統合する。

**Architecture:** `series_config.json` に manual 系列を追加。`run()` で manual はスキップ。データCSVはClaudeが日銀公式公表値から採録。build.py・index.htmlはソース非依存でCSVを読むため無変更で日本M2が表示される。

**Tech Stack:** Python 3.12（標準ライブラリ、既存 openpyxl）、Vanilla JS、pytest。**新規依存なし。**

## Global Constraints

- 配置ルート: `C:\Users\yutao\OneDrive\デスクトップ\★Claud code★\銀行一次情報`（パスは相対）。UTF-8。
- 数値は日銀公式マネーストック公表値のみを採録（推測しない・二次情報の数値は使わない）。出典を残す。
- 手動CSV形式: `date,value,yoy_pct`、`date` は `YYYY-MM-01`、`value` は M2平残（兆円）、`yoy_pct` は前年比%。
- `run()` は manual 系列を取得・上書きしない（週次無人タスクが手動値を壊さない）。
- 既存テストを壊さない。生成物（data.js・xlsx）は手編集しない。
- 既存 `fetch_macro` 関数: `load_config/load_api_key/parse_observations/compute_yoy/to_csv_text/write_series_csv/fetch_series/_parse_ecb_csv/fetch_ecb_series/get_rows/run/main`。
- Gitはmainブランチ。各タスク末尾でコミット。

---

## File Structure

| パス | 区分 | 責務 |
|---|---|---|
| `fetch_macro.py` | 変更 | `run()` が manual をスキップ、`main()` 表示、戻り値に `skipped` |
| `tests/test_fetch_macro.py` | 変更 | manualスキップ・非上書きのテスト |
| `series_config.json` | 変更 | 日本M2（manual）を追加 |
| `macro_data/JP_M2_MANUAL.csv` | 新規 | 日銀公表値（Claude採録） |
| `更新手順.md` | 変更 | 日本M2手動採録の手順 |

---

## Task 1: run() が manual ソースをスキップ

**Files:**
- Modify: `fetch_macro.py`
- Modify: `tests/test_fetch_macro.py`

**Interfaces:**
- Produces: `run(...) -> {"ok": [...], "failed": [...], "skipped": [...]}`（manual系列は `skipped` に入り、取得・上書きしない）

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` の末尾に追記:

```python
def test_run_skips_manual_source(tmp_path):
    existing = "date,value,yoy_pct\n2026-04-01,1259.0,1.5\n"
    (tmp_path / "JP_M2_MANUAL.csv").write_text(existing, encoding="utf-8")
    cfg = {"series": [{"id": "JP_M2_MANUAL", "source": "manual", "transform": "yoy_pct_also"}]}
    def boom_fred(sid, key): raise AssertionError("manualはfetchしないはず")
    def boom_ecb(key): raise AssertionError("manualはfetchしないはず")
    res = fetch_macro.run(cfg, "key", fetcher=boom_fred, ecb_fetcher=boom_ecb, data_dir=tmp_path)
    assert res["skipped"] == ["JP_M2_MANUAL"]
    assert res["ok"] == [] and res["failed"] == []
    # 既存の手動CSVが上書きされていない
    assert (tmp_path / "JP_M2_MANUAL.csv").read_text(encoding="utf-8") == existing
```

- [ ] **Step 2: テスト失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k manual -v`
Expected: FAIL（`run` が `skipped` を返さない / manualをfetchしてAssertionError）

- [ ] **Step 3: `run()` と `main()` を変更**

`fetch_macro.py` の `run` 関数を次に**置換**:

```python
def run(config: dict, api_key: str, fetcher=fetch_series, ecb_fetcher=None, data_dir: Path = MACRO_DIR) -> dict:
    if ecb_fetcher is None:
        ecb_fetcher = fetch_ecb_series
    ok, failed, skipped = [], [], []
    for s in config.get("series", []):
        sid = s["id"]
        if s.get("source") == "manual":
            skipped.append(sid)            # 手動採録: 取得も上書きもしない
            continue
        with_yoy = s.get("transform") == "yoy_pct_also"
        try:
            rows = get_rows(s, api_key, fred_fetcher=fetcher, ecb_fetcher=ecb_fetcher)
            if not rows:
                failed.append((sid, "観測値なし"))
                continue
            out = compute_yoy(rows) if with_yoy else rows
            write_series_csv(sid, out, with_yoy, data_dir)
            ok.append(sid)
        except Exception as e:  # noqa: BLE001  個別失敗はスキップして継続
            failed.append((sid, str(e)))
    return {"ok": ok, "failed": failed, "skipped": skipped}
```

`main` 関数を次に**置換**:

```python
def main() -> int:
    config = load_config()
    api_key = load_api_key()
    res = run(config, api_key)
    print(f"OK: {len(res['ok'])} series 取得")
    if res.get("skipped"):
        print(f"  手動(スキップ): {', '.join(res['skipped'])}")
    for sid, err in res["failed"]:
        print(f"  未取得 {sid}: {err}", file=sys.stderr)
    return 0
```

- [ ] **Step 4: テスト成功を確認（全件）**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS（既存の run テストは `ok`/`failed` を見ており、`skipped` 追加でも壊れない）

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: skip manual-source series in fetch (no overwrite)"
```

---

## Task 2: 日本M2を設定追加し日銀公表値を採録

**Files:**
- Modify: `series_config.json`
- Create: `macro_data/JP_M2_MANUAL.csv`

**Interfaces:**
- Consumes: Task 1 の manual スキップ。
- Produces: 反映済みの `data.js`・Excel（日本M2を含む）。

- [ ] **Step 1: series_config.json に日本M2（manual）を追加**

`series_config.json` の `series` 配列内、`ECB_M3_EUR` の項目の直後に追記:

```json
    { "id": "JP_M2_MANUAL", "country": "日本", "indicator": "マネーサプライ(M2)", "unit": "兆円", "transform": "yoy_pct_also", "source": "manual" },
```

- [ ] **Step 2: 日銀公式のマネーストック公表値を取得**

日銀マネーストック統計（月次公表）を確認し、**M2 平均残高（兆円）と前年比%** を直近約12か月分採録する。
参照: 日銀 通貨関連統計 `https://www.boj.or.jp/statistics/money/index.htm` および公表リリース（HTML/PDF）。
Expected: 直近月を含む約12行の `(年月, M2平残[兆円], 前年比%)` が手元に揃う（数値は日銀公式値のみ）。

- [ ] **Step 3: macro_data/JP_M2_MANUAL.csv を作成**

`macro_data/JP_M2_MANUAL.csv` を `date,value,yoy_pct` 形式で作成（Step 2で採録した値。`date` は各月 `YYYY-MM-01`、昇順）。例の体裁（数値はStep 2の実値に置換）:

```
date,value,yoy_pct
2025-05-01,1258.5,1.3
...
2026-04-01,1278.9,1.6
```

- [ ] **Step 4: fetch_macro が手動CSVを壊さないことを確認**

Run: `python fetch_macro.py`
Expected: `OK: 19 series 取得` と `手動(スキップ): JP_M2_MANUAL` が表示。`macro_data/JP_M2_MANUAL.csv` の内容は変化しない。

- [ ] **Step 5: 取得内容を確認**

Run: `python -c "import build; p=build.build_macro_payload(); xs=[s for s in p['series'] if s['country']=='日本' and s['indicator']=='マネーサプライ(M2)']; [print(s['latest_date'], s['latest'], s['yoy'], 'pts', len(s['history'])) for s in xs]"`
Expected: 日本M2の最新月・値・前年比・履歴点数（約12）が表示される。

- [ ] **Step 6: ビルドして反映**

Run: `python build.py`
Expected: `OK: 7 meetings, 19 calendar events`。`data.js`・Excelに日本M2が入る。

- [ ] **Step 7: Commit**

```bash
git add series_config.json macro_data/JP_M2_MANUAL.csv
git commit -m "data: add Japan M2 (manual, BOJ money stock)"
```

---

## Task 3: ドキュメント更新と最終検証

**Files:**
- Modify: `更新手順.md`

**Interfaces:**
- Consumes: Task 1/2 の成果。
- Produces: 手動採録手順の明文化と最終確認。

- [ ] **Step 1: 更新手順.md に日本M2手動採録を追記**

`更新手順.md` の「マクロ数値データ（週1回）」セクションの末尾（`### 対象シリーズの追加・修正` の直前）に追記:

```markdown

### 日本M2（手動採録）
日銀マネーストックはFRED/ECBのような安定APIが無いため手動。月次で:
1. 日銀の公式マネーストック公表（月次）でM2平残（兆円）と前年比%を確認。
2. `macro_data/JP_M2_MANUAL.csv` に最新月を `YYYY-MM-01,値,前年比` で追記（数値は日銀公式値のみ）。
3. `python build.py` → コミット。
`fetch_macro.py` は manual ソースを取得・上書きしないため、このCSVは自動更新で壊れない。
```

- [ ] **Step 2: ブラウザで最終確認**

`.claude/launch.json` の `g7-static` を起動し `index.html` を再読込。
Expected:
- 「マクロ」の「マネーサプライ(M2)」カテゴリに **米国・ユーロ圏・日本** の3カード。
- 「マネーの流れ」のM2前年比チャート・凡例に **日本** が加わる（米国・ユーロ圏・日本）。
- コンソールエラーなし。

- [ ] **Step 3: Excelと全テストを確認**

Run: `python -c "from openpyxl import load_workbook; wb=load_workbook('G7_一次情報ログ.xlsx'); ws=wb['マクロ_最新']; print([ (ws.cell(row=r,column=1).value, ws.cell(row=r,column=2).value) for r in range(2, ws.max_row+1) if ws.cell(row=r,column=1).value=='日本' and ws.cell(row=r,column=2).value=='マネーサプライ(M2)'])"`
Expected: `[('日本', 'マネーサプライ(M2)')]`（Excelに日本M2行が存在）。
Run: `python -m pytest -q`
Expected: 全PASS。

- [ ] **Step 4: Commit**

```bash
git add 更新手順.md
git commit -m "docs: manual Japan M2 update procedure"
```

---

## 完了の定義（受け入れ基準）

- [ ] `python fetch_macro.py` で日本M2が「手動(スキップ)」と表示され、手動CSVが保持される。
- [ ] `macro_data/JP_M2_MANUAL.csv` に日銀公表のM2（兆円・前年比、直近約1年）が入る。
- [ ] 「マクロ」の「マネーサプライ(M2)」に米国・ユーロ圏・日本が並ぶ。
- [ ] 「マネーの流れ」のM2比較に日本が加わる。
- [ ] Excel `マクロ_最新`/`マクロ_時系列` に日本M2が反映。
- [ ] `更新手順.md` に手動採録手順が明記される。
- [ ] `python -m pytest` 全PASS（既存を壊さない）。
