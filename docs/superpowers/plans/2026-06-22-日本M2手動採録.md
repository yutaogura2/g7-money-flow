# 日本M2 取得（BOJ自動取得）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline) to implement task-by-task. Steps use checkbox (`- [ ]`).

> **改訂**: 当初「手動採録」だったが、BOJ安定CSVを発見し「自動取得(boj ソース)」へ変更（承認済み）。manualスキップ機能（旧Task1, 実装済みcommit）は汎用機能として温存。

**Goal:** 日本M2を `source: "boj"` で追加し、BOJ主要時系列CSV（md02_m_1.csv, cp932）から自動取得してダッシュボード/Excel/週次自動更新に統合する。

**Architecture:** 既存 multi-source（fred/ecb）に `boj` を追加。`fetch_boj_m2()` がBOJ CSVのM2平残列を抽出→兆円換算→共通行形式で返す。以降は無変更で流用。

**Tech Stack:** Python 3.12（urllib・csv・io、cp932デコード）、pytest。新規依存なし。

## Global Constraints
- UTF-8（ただしBOJ CSVは cp932 デコード）。値は日銀公式CSVのみ。
- M2平残データコード `MD02'MAM1NAM2M2MO`、億円→兆円（/10000）、日付 `YYYY-MM-01`。
- 既存テスト（fred/ecb/manual）を壊さない。Gitはmain、各タスク末尾でコミット。
- 定数 `BOJ_M2_CSV = "https://www.stat-search.boj.or.jp/ssi/mtshtml/csv/md02_m_1.csv"`。

---

## Task A: BOJ CSV解析と取得（fetch_boj_m2）

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- Produces: `_parse_boj_m2_csv(text:str)->list[tuple[str,float]]`、`fetch_boj_m2(url=BOJ_M2_CSV, urlopen=...)->list`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_parse_boj_m2_csv_extracts_m2_level_in_chocho():
    text = (
        "主要時系列統計データ表\n"
        "2026/06/23 15:00\n"
        ",M2前年比,M2平残\n"
        "系列名称,M2/前年比,M2/平残\n"
        "データコード,MD02'MAM1YAM2M2MO,MD02'MAM1NAM2M2MO\n"
        "単位,%,億円\n"
        "2025/05,2.4,12660000\n"
        "2026/05,2.5,12980932\n"
    )
    rows = fetch_macro._parse_boj_m2_csv(text)
    assert rows == [("2025-05-01", 1266.0), ("2026-05-01", 1298.0932)]


def test_fetch_boj_m2_decodes_cp932(monkeypatch):
    sample = (
        "x\nx\nx\n系列名称,M2/平残\nデータコード,MD02'MAM1NAM2M2MO\n単位,億円\n"
        "2026/05,12980932\n"
    )
    class _R:
        def __init__(self, b): self._b=b
        def read(self): return self._b
        def __enter__(self): return self
        def __exit__(self,*a): return False
    def fake_urlopen(req, timeout=30):
        return _R(sample.encode("cp932"))
    rows = fetch_macro.fetch_boj_m2(urlopen=fake_urlopen)
    assert rows == [("2026-05-01", 1298.0932)]
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k boj -v`
Expected: FAIL（未定義）

- [ ] **Step 3: 実装を追加**

`fetch_macro.py` の import に `import re` を追記（無ければ）。定数 `ECB_URL = ...` の直後に追記:

```python
BOJ_M2_CSV = "https://www.stat-search.boj.or.jp/ssi/mtshtml/csv/md02_m_1.csv"
BOJ_M2_CODE = "MD02'MAM1NAM2M2MO"   # マネーストック M2 平均残高(億円)
```

`fetch_ecb_series` の後ろに追記:

```python
def _parse_boj_m2_csv(text: str) -> list:
    reader = list(csv.reader(io.StringIO(text)))
    code_row = next((r for r in reader if r and r[0] == "データコード"), None)
    if not code_row or BOJ_M2_CODE not in code_row:
        return []
    col = code_row.index(BOJ_M2_CODE)
    out = []
    for r in reader:
        if not r or not re.match(r"^\d{4}/\d{1,2}$", r[0]):
            continue
        if col >= len(r):
            continue
        val = r[col].strip().replace(",", "")
        if val in ("", "-", "ND", "NA"):
            continue
        try:
            v = float(val)
        except ValueError:
            continue
        y, m = r[0].split("/")
        out.append((f"{y}-{int(m):02d}-01", v / 10000.0))   # 億円→兆円
    out.sort(key=lambda x: x[0])
    return out


def fetch_boj_m2(url: str = BOJ_M2_CSV, urlopen=urllib.request.urlopen) -> list:
    req = urllib.request.Request(url, headers={"User-Agent": "Mozilla/5.0"})
    with urlopen(req, timeout=30) as r:
        raw = r.read()
    return _parse_boj_m2_csv(raw.decode("cp932"))
```

- [ ] **Step 4: 成功を確認 + 実APIで現行性確認**

Run: `python -m pytest tests/test_fetch_macro.py -k boj -v`
Expected: 2件 PASS。
Run: `python -c "import fetch_macro as m; r=m.fetch_boj_m2(); print('rows',len(r),'last',r[-1])"`
Expected: `rows` が数百、`last` が直近月（例 `('2026-05-01', 1298.0932)`）。

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: BOJ money-stock M2 fetcher (cp932 flat CSV)"
```

---

## Task B: get_rows/run に boj を配線

**Files:** Modify `fetch_macro.py`, `tests/test_fetch_macro.py`

**Interfaces:**
- `get_rows(series, api_key, *, fred_fetcher, ecb_fetcher, boj_fetcher=None)`、`run(..., boj_fetcher=None, ...)`

- [ ] **Step 1: 失敗するテストを追加**

`tests/test_fetch_macro.py` 末尾に追記:

```python
def test_run_handles_boj_source(tmp_path):
    cfg = {"series": [{"id": "JP_M2_BOJ", "source": "boj", "transform": "yoy_pct_also"}]}
    def fake_boj():
        return [("2025-05-01", 1266.0), ("2026-05-01", 1298.0932)]
    res = fetch_macro.run(cfg, "key", boj_fetcher=fake_boj, data_dir=tmp_path)
    assert res["ok"] == ["JP_M2_BOJ"]
    txt = (tmp_path / "JP_M2_BOJ.csv").read_text(encoding="utf-8")
    assert txt.startswith("date,value,yoy_pct")
    assert "2026-05-01" in txt
```

- [ ] **Step 2: 失敗を確認**

Run: `python -m pytest tests/test_fetch_macro.py -k boj_source -v`
Expected: FAIL（run が boj を処理しない）

- [ ] **Step 3: 実装を変更**

`get_rows` を次に置換:

```python
def get_rows(series: dict, api_key: str, *, fred_fetcher, ecb_fetcher, boj_fetcher=None) -> list:
    src = series.get("source")
    if src == "ecb":
        return ecb_fetcher(series["ecb_key"])
    if src == "boj":
        return boj_fetcher()
    return parse_observations(fred_fetcher(series["id"], api_key))
```

`run` の冒頭 `if ecb_fetcher is None: ecb_fetcher = fetch_ecb_series` の直後に追記:

```python
    if boj_fetcher is None:
        boj_fetcher = fetch_boj_m2
```

`run` のシグネチャに `boj_fetcher=None` を追加（`ecb_fetcher=None,` の後）:

```python
def run(config: dict, api_key: str, fetcher=fetch_series, ecb_fetcher=None, boj_fetcher=None, data_dir: Path = MACRO_DIR) -> dict:
```

`run` 内の `get_rows(...)` 呼び出しを次に置換:

```python
            rows = get_rows(s, api_key, fred_fetcher=fetcher, ecb_fetcher=ecb_fetcher, boj_fetcher=boj_fetcher)
```

- [ ] **Step 4: 全テスト確認**

Run: `python -m pytest tests/test_fetch_macro.py -v`
Expected: 全PASS（既存 fred/ecb/manual 含む）

- [ ] **Step 5: Commit**

```bash
git add fetch_macro.py tests/test_fetch_macro.py
git commit -m "feat: wire boj source into dispatch and run"
```

---

## Task C: 設定にJP_M2_BOJを入れ実データ取得

**Files:** Modify `series_config.json`（既存の `JP_M2_MANUAL` 行を置換）

- [ ] **Step 1: series_config.json を更新**

`series_config.json` の `JP_M2_MANUAL` の行を次に**置換**:

```json
    { "id": "JP_M2_BOJ", "country": "日本", "indicator": "マネーサプライ(M2)", "unit": "兆円", "transform": "yoy_pct_also", "source": "boj" },
```

- [ ] **Step 2: JSON妥当性**

Run: `python -c "import json; d=json.load(open('series_config.json',encoding='utf-8')); print(len(d['series']),'series')"`
Expected: `20 series`

- [ ] **Step 3: 実データ取得**

Run: `python fetch_macro.py`
Expected: `OK: 20 series 取得`（FRED17+ECB2+BOJ1）。`macro_data/JP_M2_BOJ.csv` 生成。

- [ ] **Step 4: 取得内容とYoY整合を確認**

Run: `python -c "import build; p=build.build_macro_payload(); s=[x for x in p['series'] if x['id']=='JP_M2_BOJ'][0]; print(s['latest_date'], round(s['latest'],1), s['yoy'], 'pts', len(s['history']))"`
Expected: 最新月・約1298（兆円）・前年比（日銀公表の+2.5%付近）・履歴点数（長期）。

- [ ] **Step 5: ビルド**

Run: `python build.py`
Expected: `OK: 7 meetings, 19 calendar events`。

- [ ] **Step 6: Commit**

```bash
git add series_config.json macro_data/JP_M2_BOJ.csv
git commit -m "data: add Japan M2 via BOJ flat CSV (auto)"
```

---

## Task D: ドキュメントと最終検証

**Files:** Modify `更新手順.md`

- [ ] **Step 1: 更新手順.md を更新**

`更新手順.md` の「### 日本M2（手動採録）」セクションがあれば削除し、「### 対象シリーズの追加・修正」の直前に追記（無ければ「マクロ数値データ（週1回）」末尾に追記）:

```markdown

### 日本M2（BOJ自動取得）
日銀の主要時系列フラットCSV（`md02_m_1.csv`, cp932）からM2平残を自動取得（`source:"boj"`）。
週次タスクで自動更新されるため手動作業は不要。列はデータコード `MD02'MAM1NAM2M2MO` で特定し、億円→兆円換算。
```

- [ ] **Step 2: ブラウザ確認**

`g7-static` 起動、`index.html` 再読込。
Expected: 「マクロ」の「マネーサプライ(M2)」に米国・ユーロ圏・日本の3カード。「マネーの流れ」M2比較に日本が加わる。コンソールエラーなし。

- [ ] **Step 3: Excel・全テスト確認**

Run: `python -c "from openpyxl import load_workbook; wb=load_workbook('G7_一次情報ログ.xlsx'); ws=wb['マクロ_最新']; print([(ws.cell(row=r,column=1).value,ws.cell(row=r,column=2).value) for r in range(2,ws.max_row+1) if ws.cell(row=r,column=1).value=='日本' and ws.cell(row=r,column=2).value=='マネーサプライ(M2)'])"`
Expected: `[('日本', 'マネーサプライ(M2)')]`
Run: `python -m pytest -q`
Expected: 全PASS。

- [ ] **Step 4: Commit**

```bash
git add 更新手順.md
git commit -m "docs: Japan M2 via BOJ auto-fetch"
```

---

## 完了の定義
- [ ] `python fetch_macro.py` で日本M2が自動取得され `JP_M2_BOJ.csv` に保存。
- [ ] 「マクロ」M2に米・ユーロ圏・日本、「マネーの流れ」M2比較に日本。
- [ ] Excelに日本M2反映。週次タスクで自動更新。
- [ ] `python -m pytest` 全PASS。
